# 像素蛋挞 2.3.0 RC3 日历资料与收支修复报告

结论：RC3 运行时主题、专业工作日历、拍摄资料预览、本地摄影收支与联机空状态整理已经完成；最终代码、专项回归、全量回归、真实 WPF 证据、RC3 新装和 RC2 升级验收均通过。物理双屏仍按约定挂起。

## 1. 开始 HEAD

- 用户给出的 RC2 代码合并点：`d3d167f953294b87bb4b69d07ad9860b00a05759`。
- 本轮实际创建修复分支时，`release/2.3.0` 的最新干净 HEAD 为 `f7d1ae819a2a41118e106b65bf9f90af39c2e7f7`，它是在上述 RC2 合并点之后追加的 RC2 UI 验收报告提交。
- 因此本报告以实际开始 HEAD `f7d1ae819a2a41118e106b65bf9f90af39c2e7f7` 为准。

## 2. 修复分支

`fix/2.3.0-rc3-calendar-finance-runtime-ui`

## 3. 最终 HEAD

- 功能分支最终 HEAD：`716d0f40bdd64bce6986f7acdbad167f1246e372`。
- 最终产品代码合并 HEAD：`fe5de3dfdd9f5f865049287352eb5d89249a29fe`。
- RC3 安装包的正式二进制源提交：`b2a5f017930f4c54f26416ac79658ba6d8d7f925`；其后的提交只稳定隔离验收脚本，不改变安装包产品代码。

## 4. 合并提交

已使用正常非快进合并回 `release/2.3.0`：`fe5de3dfdd9f5f865049287352eb5d89249a29fe`，提交信息为 `merge: complete 2.3.0 RC3 calendar finance and runtime fixes`。

## 5. ProductVersion

- ProductVersion：`2.3.0`
- FileVersion：`2.3.0.0`
- OutputType：`WinExe`
- Provider：`None`

## 6. SchemaVersion

`SchemaVersion = 4`。

## 7. 是否新增迁移

是。迁移名为 `BookingPeopleAndFinanceMvp`，仅新增四张授权业务表：

- `BookingContacts`
- `BookingStaffMembers`
- `FinanceCategories`
- `FinanceTransactions`

迁移沿用现有备份、事务、回滚、`integrity_check`、幂等与高版本保护机制。RC2 隔离升级实测从 Schema 3 成功到 Schema 4，旧项目、排期、提醒、文档和联机会话均保留。

## 8. DatePicker 根因

原控件只覆盖了部分输入外观，运行时弹出的 `Calendar`、日期按钮、年月按钮和导航按钮仍可能使用 Windows 默认浅色模板；因此深色主题下出现白色 Popup、浅蓝系统按钮与低对比文字。所有日期控件共享同一资源时，这种缺口会在创建排期等真实弹窗中暴露。

## 9. 日期 Popup 修复

在 `Controls.Inputs.xaml` 中补齐统一的 `DatePicker`、`DatePickerTextBox`、`Calendar`、`CalendarItem`、`CalendarDayButton` 与 `CalendarButton` 模板。背景、边框、文字、今日、选中、悬停、禁用、跨月、键盘焦点和导航按钮均使用动态主题资源；深色、浅色、高对比证据分别通过。

## 10. 其他主题泄漏

已统一 ComboBox Popup、箭头与选择内容模板，保留 `DisplayMemberPath` 的中文显示；ContextMenu、ToolTip、Toast/消息对话框改用设计系统资源。Toast 采用带边框的主题卡片和文字/图标语义，不只依赖颜色，也不占用工作台右上日历关键操作区。

## 11. 图钉状态

未固定使用中性空心图钉与“固定到工作台快捷区”；已固定使用强调色实心图钉、`已固定` 标签与“从工作台快捷区取消固定”。工具名和状态进入无障碍名称，切换后工具箱与工作台快捷区立即刷新。

## 12. 本地分片问号

问号与“开始本地分片”标题放在同一水平布局，间距为紧凑设计值，不再漂到卡片右上角。鼠标悬停和键盘聚焦显示说明，支持自动换行、最大宽度、合理显示时长和 Escape 关闭；卡片 Hover 不再使用过曝的浅蓝底白字。

## 13. 工作台三组图例

已完成固定三组主图例并带中文文字：`未拍摄`、`已拍摄`、`待返图`。不再显示三个无说明图标。

## 14. 图例实际语义颜色

- 深色主题：未拍摄使用 `AccentBrush #C98220`，已拍摄使用 `SuccessBrush #56C78B`，待返图使用 `WarningBrush #F3B65C`。
- 浅色主题：强调色保持 `#C98220`，成功色为 `#16794B`，警告色为 `#9A5A00`。
- 高对比主题：使用 Windows 系统高对比色，同时保留文字标签和状态图形，不只靠颜色表达。

## 15. 空闲日期

空闲日期不再绘制灰色圆点、数量 0 或伪状态。只有存在排期时才显示主状态条/点、数量及必要的冲突或天气标记；今天、选中和关闭档期使用独立边框或背景语义。

## 16. 完整日历布局

已重构为顶部控制栏与可折叠完整图例、左侧大日历/列表/总览、可拖动分隔线、右侧当天详情。默认网格比例为 65/35，并为左右区域设置最小宽度；右侧不再是无操作的大空白，固定提供当天统计、任务卡片与创建入口。

## 17. 视图切换

支持日历视图、列表视图、总览视图，并保留月/周/日时间尺度切换；年月导航、返回今天与快捷键均接入同一 ViewModel 状态。

## 18. 状态筛选

支持中文状态、类型、排序、搜索范围与关键词筛选。用户界面通过显示模型和转换器输出中文业务文案，不暴露内部枚举；搜索覆盖项目、客户/模特、联系方式、地点、拍摄内容等本地字段，搜索内容不写入普通日志。

## 19. 右键菜单

日期格提供主题化菜单：创建拍摄任务、关闭本日档期、打开本日档期、本日设置、查看当天详情；未加入需要云能力的邀请预约。

## 20. 关闭档期

关闭档期只写入本地可用性状态，不删除或修改已有任务。日期已有任务时先确认，关闭后显示明确状态，并可重新开放；没有新增额外数据库表。

## 21. 当天详情

右侧显示日期、年月/星期、任务总数、未拍摄、已拍摄、待返图和冲突统计。空状态提供“创建当天拍摄任务”；任务卡显示项目、客户代号、时间、地点、状态、天气及查看策划/收支入口。

## 22. 联系方式

排期四步编辑流程加入多联系人：客户或模特代号、手机、微信、邮箱、其他联系方式、主要联系人和备注。联系人靠近客户名称显示，紧凑显示可脱敏，客户监看默认不展示联系方式；新装和升级探针均验证重启持久化。

## 23. 工作人员

支持多工作人员的姓名/代号、角色、到场时间、手机、微信、邮箱、备注和排序；支持增加、编辑、移除关联及上下移动。移除只影响当前排期关系，不删除文件，也不修改其他排期。

## 24. 文件上传

复用既有 `BookingDocumentsPanel`、工作流服务和文件安全链路，支持文件选择、拖拽、多选、默认仅关联原位置、显式安全复制、重新定位和移除关联。文件夹不会递归扫描；安全复制不覆盖、不移动、不删除源文件，复制成功后才保存关系。

## 25. 图片预览

JPG/JPEG/PNG/WEBP/BMP/GIF/TIF/TIFF 使用本地只读内嵌预览，支持 Fit、100%、缩放和前后切换。加载使用 `BitmapCacheOption.OnLoad`，读取完成即释放流，不长期锁定原文件。

## 26. PDF 预览

使用 Windows 本地 PDF API 渲染页面，支持页码、上一页、下一页、缩放、Fit 和外部打开。渲染后的位图脱离输入流，预览失败只回退为安全状态，不影响关联保存或原文件。

## 27. Office 文件边界

DOC/DOCX/XLS/XLSX/PPT/PPTX 不解析正文、不执行宏或嵌入脚本、不联网、不安装第三方 Office 套件；显示安全文件卡片，并允许用户主动使用系统默认应用打开。PSD、AI、ZIP、RAR 和未知格式同样只显示元数据卡片。

## 28. 创建排期分步流程

创建窗口改为四步：基础信息、时间与准备、策划资料、联系人/工作人员/收支。支持保存草稿和基础排期，后续资料可稍后补充；底层窗口模态禁用并暗化，关闭时回到原日期。技术枚举、UTC 边界和内部实现名不直接暴露给用户。

## 29. 收入与支出

新增左侧“收支”入口和本地摄影业务记账 MVP。支持收入/支出、默认分类、金额/币种、日期、支付状态、关联排期/项目、对方、支付方式、备注、附件引用、新建、编辑、复制、取消、确认删除、筛选、搜索和主动导出 CSV。

## 30. 财务汇总

提供本月收入、本月支出、本月净现金流、待收、待付、项目预计利润与最近交易；排期侧显示预计收入、已收、未收、支出、待付和净收益。拍摄或交付状态不会自动修改收付款状态。

## 31. 财务隐私

财务数据默认只保存在本机。普通日志和诊断不记录金额正文、交易对方、联系方式、财务备注或附件路径；CSV 仅在用户主动操作后输出。模块不联网、不自动同步、不自动报税，也不宣称替代专业会计系统。

## 32. 联机工具栏

整理重复入口，同一动作只保留一个主要入口；工具栏采用紧凑布局和溢出入口，避免自然换行。无照片时全屏不可进入无反馈黑屏，技术状态转为中文用户提示。

## 33. 联机空状态

未启动时突出“选择看守文件夹”和“开始联机会话”；运行但无照片、已有照片未选择分别显示明确文案。检查器在空状态只展示必要会话区，隐藏无意义的标记/备注密集内容。

## 34. 新增测试

测试从 1201 项增加到 1416 项，最终组成：Core 1010、WPF 319、DPI 87。新增或强化主题、图钉、问号、迷你日历、完整日历、联系人、工作人员、资料预览、四步流程、财务、联机空状态、Schema 3→4、隐私和源文件安全覆盖；另加入“工作日历延迟模板必须自带状态转换器资源”的重启回归测试。

## 35. 最终测试总数

`1416/1416`，失败 0、跳过 0、警告 0、错误 0。

## 36. 重复回归

以下专项全部按要求通过：日期 Popup 20 轮、图钉 20 轮、迷你日历 20 轮、完整日历 20 轮、创建排期 20 轮、资料预览 10 轮、收支核心 20 轮、联机空状态 20 轮、页面互斥 20 轮、Schema 迁移 20 轮、Core 并行 3 轮、Core 非并行 3 轮。运行时转换器修复后没有重复这些已完成专项，只对最终代码重新执行 Debug/Release 全量各 3 轮。

## 37. Debug 结果

- 构建：0 警告、0 错误。
- 全量第 1/2/3 轮：均为 `1416/1416`，失败 0、跳过 0。
- 证据：`artifacts/test-results/2.3.0-rc3-post-runtime-fix/`。

## 38. Release 结果

- 构建：0 警告、0 错误。
- 全量第 1/2/3 轮：均为 `1416/1416`，失败 0、跳过 0。
- 发布前 Release 构建、测试和发布再次通过，发布扫描确认 WinExe、Provider None、无 Fake Camera、无厂商 SDK、无 localhost、无测试/日志/数据库/合成素材进入安装包。

## 39. 原 1201 项是否保留

是。最终总数增加为 1416，没有删除、禁用或跳过原测试。

## 40. UI 证据

- 目录：`D:\AI AGENT\RAWSelectionAssistant\artifacts\ui-review\2.3.0-rc3`
- 44/44 张真实 WPF 运行截图，44 个唯一 SHA-256，布局失败 0，阻断问题 0。
- 包含深色/浅色/高对比、1280 宽度、150% 与 200% DPI。
- 总览：`D:\AI AGENT\RAWSelectionAssistant\artifacts\ui-review\2.3.0-rc3\像素蛋挞_2.3.0_RC3日历资料与收支UI总览.png`

## 41. 交互日志

`interaction-log.jsonl` 记录每个运行状态的时间、进程、页面、焦点、Popup 预期、截图和异常。实际人工点击覆盖工作台、本地分片帮助、工具箱固定/取消固定、收支页、收入表单、下拉框和升级教程关闭；隔离安装 UI Automation 继续实际打开工作台、日历、收支和联机页。其余复杂状态使用真实 WPF 状态驱动捕获，不读取用户真实资料。

## 42. 录屏或替代证据

没有伪造 MP4。替代证据为 44 张连续真实 WPF 截图、逐张元数据、UI Automation 轨迹、人工点击摘要，以及 RC3 新装/升级的独立桌面 UI 树。真实点击曾发现升级教程非模态关闭异常并完成修复复验。

## 43. RC3 安装包

`D:\AI AGENT\RAWSelectionAssistant\artifacts\releases\2.3.0\installer\像素蛋挞_Setup_2.3.0_RC3_x64.exe`

安装包为自包含 `win-x64` RC3，未生成无 RC 后缀正式包。签名状态为 `NotSigned`，因为未配置正式签名证书。

## 44. RC3 大小

`49,989,112` 字节。

## 45. RC3 SHA-256

`B3AD224E75394C0219FFC583A8965DE39BF38176ED1AE61BC185D60C8A225876`

早期候选包 `219901346B5A50FCDDA6870807B6EF73C1480F585427E388C53C1CBC0282956A` 因真实数据重启暴露 `StatusToTextConverter` 资源作用域缺陷，已按原哈希移入诊断留档，未作为最终 RC3 发布。最终包来自修复后的提交。

## 46. RC1 和 RC2 是否保留

是，名称、大小和 SHA-256 均未改变：

- RC1：`7C9AD2689BBCC5960D7B20396D8951D63F012A615447FEAE453BC6CABD588A2C`
- RC2：`E26050081A9D1AC45D5A4B6B7B43FFE0F835B1898350CCA85DF53B1F33EBFA91`

## 47. 隔离安装

RC3 全新安装通过。使用独立 LocalAppData 和隔离桌面；安装、启动、工作台、三组图例、固定工具、日历、收支、联机空状态、深色配置、Schema 4、四张新表、22 个默认分类、联系人、工作人员、两笔收支、资料关联、带业务数据重启、源文件哈希、卸载和用户数据保留均通过。

最终新装证据 RunId：`153e722db3b546cf9568bd91e5729c81`。

## 48. RC2 升级

RC2→RC3 隔离升级通过。受控 Schema 3 数据包含项目、排期、提醒、文档、Tether 会话和设置；升级后 Schema 4、`integrity_check=ok`、旧数据、文档关联、提醒、Tether、设置和源文件哈希全部保留，新联系人、工作人员与收支可创建并重启读取。

最终升级证据 RunId：`44810cf0c7824019967039eb0546d7ac`。

## 49. 是否物理双屏

否。本轮只有一块真实显示器，未执行、未伪造、未重复虚拟双屏验收。物理双屏最终验收继续挂起至用户回家连接第二块真实显示器。

## 50. 是否合并 main

否。

## 51. 是否创建 Tag

否，未创建 `v2.3.0` Tag 或其他封版 Tag。

## 52. 是否进入 2.4.0

否。未合并 `planning/2.4.0-camera-sdk`，未下载厂商 SDK，未开始 2.4.0 产品开发。

## 53. 已知限制

- 物理双屏仍待真实第二显示器验收。
- Office 文件只提供安全信息卡和系统打开，不在应用内解析正文。
- 收支是本地交易记录 MVP，不含支付、发票、云同步或总账。
- 安装包未签名，因为没有配置生产证书。
- 完整日历默认 65/35，可拖动分隔线；窄窗口仍以双方最小宽度保护可用性。
- 未生成真实 MP4；以连续真实 WPF 截图、元数据、UI Automation 轨迹和隔离安装 UI 树替代。

## 最终状态

- RC3 功能：完成。
- 最终测试：1416/1416，Debug 与 Release 各 3 轮全通过。
- 最终安装包：已生成并通过新装、升级和带数据重启验收。
- release/2.3.0：已完成非快进合并。
- main、Tag、2.4.0、物理双屏：均未执行。

# 像素蛋挞 2.3.0 物理双屏人工验收清单

## 使用条件

- 使用两台或更多真实物理显示器，不使用虚拟显示器冒充。
- 从 `release/2.3.0` 对应的 RC1 安装包安装，先核对 SHA-256：`7C9AD2689BBCC5960D7B20396D8951D63F012A615447FEAE453BC6CABD588A2C`。
- 只使用独立测试目录、合成 JPG/RAW、合成 LUT，不使用客户资料或用户真实照片。
- 测试前记录每台显示器的分辨率、DPI、方向、StableKey 与 ICC 状态。

## 验收项目

1. 在第二台物理显示器打开客户监看窗口，确认主窗口仍留在主屏。
2. 验证“跟随主选中”，连续切换素材时客户屏显示与主选中一致。
3. 验证“跟随最新”，向看守目录写入合成素材后客户屏显示最新 Ready 资产。
4. 验证“独立锁定”，主屏继续切图时客户屏保持锁定内容。
5. 验证客户收藏与客户备注可保存、重启后仍存在。
6. 验证 LUT 开关、强度、前后对比和分屏在主屏与客户屏同步且不写回源文件。
7. 为两台显示器配置不同的现有 ICC，验证各自使用对应配置；不得安装或修改系统 ICC。
8. 验证客户屏全屏、退出全屏、关闭和重新打开。
9. 切换客户窗口目标显示器，确认 StableKey 选择不依赖显示器数组顺序。
10. 改变主选中、分辨率、DPI 或屏幕方向后，确认窗口布局和显示状态合理恢复。
11. 环境允许时禁用或拔出客户屏，确认客户窗口进入明确断开状态，Watch Folder 会话不中断。
12. 重新连接客户屏，确认目标屏恢复、ICC 刷新且不产生重复资产。
13. 确认客户屏默认隐藏路径、文件名及其他敏感信息，且无删除、移动、覆盖等文件危险操作。
14. 验证横屏/竖屏、1080p/4K、混合 DPI 场景至少各一组。
15. 检查主屏与客户屏均无裁切、白色过曝输入框、调试边框或布局辅助线。
16. 验证关闭客户窗口不会停止联机会话，停止联机会话后客户窗口状态清晰。
17. 最后核对所有合成源文件 SHA-256 不变、队列归零、无重复资产、无 NeedsAttention 遗留。

## 通过后的封版动作

- 将 `PhysicalSecondMonitorTested` 更新为 `true`，记录显示器拓扑和逐项结果。
- 重新从已验收提交生成正式名称 `像素蛋挞_Setup_2.3.0_x64.exe`，不得覆盖 RC1。
- 重新计算安装包 SHA-256，复跑隔离安装和升级验收。
- 工作树干净后才允许合并 `main` 并创建带注释的 `v2.3.0` Tag。
- 未完成以上动作前，不得宣称真实物理双屏通过，也不得进入 2.4.0。

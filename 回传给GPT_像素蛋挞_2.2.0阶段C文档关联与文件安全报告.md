# 像素蛋挞 2.2.0 阶段 C：文档关联与文件安全报告

## 结论

阶段 C 已完成。排期详情现已具备本地文档引用、可选安全复制、文件状态检查、重新定位、移除关联、数据库写入失败恢复、UndoJournal 安全撤销和隐私脱敏能力。阶段 C 未修改数据库结构或 SchemaVersion，未实现任何阶段 D 或后续功能。

## 1. Git 基线

- 仓库路径：`D:\AI AGENT\RAWSelectionAssistant`
- 开发分支：`release/2.2.0`
- 恢复核验时 HEAD：`5d4e0c1f466e9e97428f660756c8669b2f7e9a82`
- 阶段 A、阶段 B 提交对象均存在，且均为恢复核验时 HEAD 的祖先。
- 恢复核验时工作树并非干净：存在 23 个文件组成的未提交阶段 C 工作集；只读检查确认它们全部属于文档工作流、文档面板、文件安全复用和阶段 C 专项测试，没有无关功能、Schema 迁移或未来阶段内容，因此修改来源明确，可以安全续接。
- 最近阶段 B 测试记录为 `698/698`；恢复后重新编译并排除阶段 C 新测试重跑原基线，结果仍为核心 `642/642`、WPF `29/29`、DPI `27/27`，合计 `698/698`。
- 产品版本：`2.2.0`
- 文件/程序集版本：`2.2.0.0`
- `main` 仍停留在 `13e48eb26ce710bbd0845a062953ccc21e03957d`，未合并阶段 A、B、C。

## 2. 阶段 A 提交

`49246dbdb5717e43f4dda3cc6f2acdb084c26679`

## 3. 阶段 B 提交

`b54bd0d7d850f4811ce196094a78b0b18878ceaa`

## 4. 阶段 C 提交

- 功能提交：`db14bf36e58c545e262966f60d23b81eedaed37a`
- 安全加固提交：`6aa1cd6d5f5b5b117287ad28201a2b5db38111a3`
- 候选修改审计补齐提交：`5973e3f1e8b1f9075bb1755a02a7587244cf8f47`

功能提交信息：`feat(calendar): add safe booking document links`

安全加固补充了混合批次部分完成、规范化重复输入、任务最终状态、重新定位原子更新、归档重试保护和跨排期待处理隔离；候选修改审计补齐提交增加了完整的重新定位持久化验证、参数化 SQL 约束验证和重试审计 ProjectId 一致性，不修改阶段 A 或阶段 B 提交。

## 5. 当前分支和 HEAD

- 当前分支：`release/2.2.0`
- 阶段 C 代码最终检查点 HEAD：`5973e3f1e8b1f9075bb1755a02a7587244cf8f47`
- 本报告通过独立文档提交保存；报告提交只更新本 Markdown，不修改阶段 C 代码检查点。

## 6. 工作树状态

- 恢复核验时：存在来源明确的阶段 C 未提交工作集，无来源不明修改。
- 阶段 C 功能提交、安全加固提交和最终报告提交完成后：要求并复核为干净。
- 未执行重置、强制清理或删除用户文件；未使用 `git reset --hard`。

## 候选修改只读审计补充

- 审计开始时发现三个来源待确认文件：`BookingContracts.cs`、`BookingDocumentWorkflowService.cs`、`SqliteBookingDocumentRepository.cs`。
- 审计期间发现与其配套的任务桥接、ViewModel 和测试修改，随后由外部进程形成提交 `6aa1cd6d5f5b5b117287ad28201a2b5db38111a3`；本次未重置、覆盖或改写该提交。
- 保留的有效修改包括：仓储接口和参数化 SQLite 实现、路径/NormalizedPath/大小/修改时间/可选哈希/IsMissing/LastVerifiedAtUtc 原子更新、重新定位重复目标保护、哈希不一致确认、混合文件批次部分完成、权威排期 ProjectId、任务 Failed/NeedsAttention/PartiallyCompleted 结果、归档重试保护及跨排期待处理隔离。
- 未发现阶段 D 代码：没有提醒调度、错过提醒补报、工作台今日拍摄或未来 7 天实现。
- 未修改 SchemaVersion、数据库迁移、表、字段、索引或约束，也未创建 `ProjectRelationships`。
- 候选修改中没有直接 `File.Delete`、`File.Move`、`File.Copy`、回收站或覆盖用户文件逻辑；移除关联仍只删除 SQLite 关系记录。
- 接口、实现和调用方一致；重新定位使用单条参数化 SQL 同步写入位置、元数据、哈希和验证状态。
- 为候选修改补齐了重新定位全部字段、参数化 SQL 和权威 ProjectId 专项测试，全部通过。

## 7. BookingDocumentsPanel

- 新增独立的 `BookingDocumentsPanel`，嵌入拍摄详情区域。
- 新增 `BookingDocumentsViewModel`，页面和 ViewModel 均不直接访问 SQLite。
- ViewModel 不直接执行文件复制、移动或删除；文件复制统一进入现有文件任务基础设施。
- 面板显示分类、显示名称、扩展名、状态、关联方式、添加时间、最近验证时间。
- 面板提供添加引用、安全复制、拖入、打开、打开所在位置、检查、重新定位、移除关联和完整路径展开操作。
- 已归档排期只读，不能添加、重新定位或移除关联。

## 8. 文档分类

已实现八类：

- 摄影策划
- 拍摄协议
- 模特授权书
- 报价单
- 场地资料
- 服装参考
- 灯光图
- 其他

支持扩展名：PDF、DOC、DOCX、PPT、PPTX、XLS、XLSX、TXT、JPG、JPEG、PNG。未读取、索引或分析文档正文。

## 9. 默认引用模式

- 默认操作为“仅关联原位置”。
- 仅保存既有 `BookingDocuments` 表中的路径和元数据字段。
- 不复制、不移动、不重命名、不删除电脑文件。
- 同一排期内相同规范路径由数据库唯一约束和仓储查询共同阻止重复写入，并返回友好跳过结果。
- 文档、图片、PDF、Office 文件、RAW、缩略图和其他二进制均不写入 SQLite。

## 10. 拖入行为

- 支持一次拖入单个或多个文件。
- 拖入后先显示三种选择：“仅关联原位置（默认）”“复制到项目资料目录并关联”“取消”。
- 不会因拖入而自动复制。
- 拖入文件夹会被拒绝并提示当前版本只支持单个或多个文件，不会递归扫描。
- 对话框关闭后恢复原焦点。
- 批处理状态显示总数、成功、失败、跳过和等待确认。

## 11. 文件复制安全

- 复用 2.1.0 的 `TaskEngine`、`FileOperationPlan`、`FileOperationValidator`、`FileOperationExecutor`、`FileVerificationService`、`NotificationCenter`、`ErrorCodeCatalog` 和 `AuditLog`。
- 操作类型固定为 Copy；冲突策略固定为 AutoNumber。
- 目标写入使用 CreateNew，不覆盖同名文件。
- 写入后执行异步 Flush 和物理 Flush。
- 校验文件长度；界面发起的复制默认附带 SHA-256 校验。
- 源文件不移动、不删除。
- 权限不足、目标空间不足、文件占用、路径问题和目标不可写均返回安全失败或 NeedsAttention，不创建文档关联。
- 混合批次按文件独立验证；单个坏文件不会阻止其他有效文件安全复制，结果进入 PartiallyCompleted。
- 不允许复制到安装目录或应用数据目录。

## 12. FileOperationPlan

- 每次托管复制先生成不可变 FileOperationPlan，再交给现有执行器。
- 目标目录不存在时由现有计划/执行链安全创建。
- 存在项目 OutputDirectory 时建议 `<OutputDirectory>\拍摄资料\<文档分类>`。
- 无关联项目或 OutputDirectory 为空时要求用户选择目录；选择结果只作为本次排期会话偏好，不修改项目记录。
- 未建立第二套文件复制系统。

## 13. 数据库关系写入

- 只有文件复制和验证成功后才写入 `BookingDocuments` 关联。
- 关联记录继续只保存路径、元数据、关联方式和可选哈希。
- 仓储新增按排期与规范路径查询、跨排期规范路径查询，以及路径、元数据和哈希的单条 SQL 原子更新能力。
- 没有新增表、字段、索引、迁移或 Schema 版本变化。

## 14. NeedsAttention

- 文件复制成功但关联写入失败时，不标记为完全成功。
- 用户会看到文件已复制、关联记录未保存、源文件仍安全。
- 提供重试保存关联、打开输出目录、安全撤销本次复制、保留文件但放弃关联四种恢复操作。
- 目标空间不足等需要人工处理的计划级错误会准确传递为 NeedsAttention。
- 没有产生输出的纯失败任务进入 Failed；已复制但数据库关系待处理的任务进入 PartiallyCompleted，避免误报 Completed。

## 15. PartiallyCompleted

- 复制成功但数据库写入失败时，任务通过现有 TaskEngine 进入 `PartiallyCompleted`。
- 批处理结果保留 `PendingDocumentAssociation`；当前应用会话内按 BookingId 隔离待处理动作，切换排期不会串行显示到其他拍摄。
- Task、OperationItem 和 UndoJournal 持久化安全复制结果；本阶段未新增数据库表。
- 任务进度和摘要不持久化客户名、文档名、文件名或完整路径。

## 16. UndoJournal

- 安全撤销复用现有 UndoJournal，并新增按任务和目标文件精确撤销能力。
- 撤销前验证目标确由该任务创建、大小与哈希未变化、关联未被当前或其他排期占用。
- 任一条件不满足时不删除文件，返回等待确认。
- 撤销不会删除源文件，也不会影响同一任务的其他输出。
- 普通“移除关联”不会自动调用 UndoJournal。

## 17. 文件丢失

- 打开拍摄详情时只检查当前排期文档。
- 用户打开、打开所在位置、主动检查时再次验证目标文件。
- 文件不存在、外接盘断开或当前不可访问时设置 `IsMissing=true` 和 `MissingSinceAtUtc`，保留关联且不使页面崩溃。
- 不后台扫描全部历史文档路径。

## 18. 重新定位

- 用户选择新文件后更新文件路径、规范路径、扩展名、大小、修改时间和最近验证时间，并清除缺失状态。
- 旧记录无哈希时可直接重新定位。
- 旧记录有哈希时先比较 SHA-256；一致则接受，不一致则要求用户选择取消或接受新文件。
- 哈希不一致不会自动接受；用户明确接受后才更新路径与哈希。
- 重新定位到当前拍摄已关联的另一文件会被拒绝，不覆盖两条现有关系。
- 接受哈希不一致时，路径、元数据和新哈希通过单条数据库更新一起提交，避免半更新状态。

## 19. 移除关联

- 按钮名称为“移除关联”。
- 固定提示为：“仅从当前拍摄中移除关联，不会删除电脑中的原文件。”
- 仅删除 `BookingDocuments` 关系并刷新当前详情、写入脱敏审计。
- 不删除 Reference 文件，不删除 ManagedCopy 文件，不移入回收站，不删除空目录，不修改项目文件夹。
- 已归档排期不能移除文档关联。

## 20. 归档和恢复

- 归档排期保留全部文档关联。
- 归档状态下文档区域只读，但仍可查看、打开和检查。
- 恢复排期后原文档关联完整显示，并重新允许阶段 C 范围内的修改操作。

## 21. 隐私与日志

- 阶段 C 审计正文只记录 BookingId、DocumentType、Operation 和 Result；TaskId、ProjectId、ErrorCode、CorrelationId 使用既有结构化字段。
- 不记录客户姓名、电话、显示名称、文件名、完整路径、文档哈希或正文。
- 审计与诊断脱敏器会隐藏 Windows 路径、文件名/显示名称字段和 64 位哈希。
- 通知、错误提示和任务进度使用通用描述，不写入敏感名称或完整路径。
- 完整路径默认不显示，只有用户主动展开时显示。

## 22. DPI 和主题

- 面板使用现有动态主题资源，支持浅色、深色和高对比主题体系。
- 文档列表可滚动，长名称使用省略显示，操作按钮使用可换行布局。
- 文档面板纳入 125%、150%、200% 隔离逻辑 DPI 视口自动化测量。
- 未要求用户切换 Windows 缩放或人工截图。

## 23. 无障碍

- 拖入区域设置 AutomationProperties.Name 和 HelpText。
- 文档分类、顶部操作、每个文档操作及数据库失败恢复操作均设置明确辅助名称。
- 状态同时显示文字，不只依赖颜色。
- 所有操作均为标准可聚焦按钮或组合框，可通过键盘 Tab 顺序访问。
- 拖入处理选择窗口的默认和取消按钮完整；窗口关闭后恢复原焦点。

## 24. 测试总数

- Debug：`759/759` 通过。
- Release：`759/759` 通过。
- 核心测试：`693/693`。
- WPF 测试：`39/39`。
- DPI 测试：`27/27`。
- 相比阶段 C 开始的 698 项基线，新增 61 项阶段 C 专项测试；原 698 项全部保留并通过。
- 覆盖默认引用、单/多文件、混合有效/无效批次、规范化重复路径、分类与元数据、无 BLOB、正文不读取、拖入选择、文件夹拒绝、安全复制、CreateNew、AutoNumber、长度/SHA-256、写入顺序、NeedsAttention、Failed、PartiallyCompleted、UndoJournal、修改后拒绝撤销、缺失/断盘、重新定位、重复目标拒绝、哈希一致/不一致、原子更新、移除不删文件、归档恢复、归档状态重试保护、跨排期待处理隔离、中文/空格/长路径、文件占用、权限不足、空间不足、隐私、架构边界、主题/DPI/无障碍和 Release 禁止项。
- 所有真实文件操作测试均使用独立临时目录，未使用用户真实文档、照片或正式数据库。

## 25. 构建结果

- Debug 构建：通过，0 错误、0 警告。
- Release 构建：通过，0 错误、0 警告。
- 未执行 Publish。

## 26. 数据库是否变化

否。阶段 C 未修改数据库结构、迁移文件、表、字段、索引或约束。

## 27. SchemaVersion

仍为 `2`。日历迁移仍只创建以下四张表：

- `ShootBookings`
- `ShootRequirementItems`
- `BookingDocuments`
- `BookingReminders`

未创建 `ProjectRelationships`。

## 28. 未实现范围

阶段 C 未实现：提醒调度、错过提醒补报、工作台今日拍摄、未来 7 天、ProjectRelationships、多项目关系、永久删除排期、项目模板、项目状态机、项目健康检查、本地选片、精修片回匹配、联系表、交付包、文件夹监听、CRM、在线预约、支付和云同步。

## 29. 是否生成安装包

否。未执行 Publish，未生成正式安装包。

## 30. 是否合并 main

否。全部修改仅提交到 `release/2.2.0`。

## 31. 是否创建 Tag

否。未创建 `v2.2.0` Tag 或其他 Tag。

## 32. 是否建议进入阶段 D

不自动建议立即进入。阶段 C 已满足代码、构建、测试、数据库边界和文件安全门禁，可以提交人工审查；只有在用户明确验收并另行下达阶段 D 指令后才进入阶段 D。本次在阶段 C 完成后立即停止。

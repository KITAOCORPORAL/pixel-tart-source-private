# 像素蛋挞 2.2.0 阶段B工作日历界面报告

## 1. 阶段结论

阶段B“工作日历界面与归档恢复”已完成，并已停在阶段B边界。

本阶段完成独立工作日历入口、月/周/日视图、当前视图查询、全部未归档分页搜索、当天列表、排期详情、新建和编辑、准备清单、金额警告、冲突三选项、归档查看与恢复。未进入阶段C，未开发正式文档关联界面、提醒调度或其他后续能力。

## 2. 阶段A检查点提交

- 阶段A提交：`49246dbdb5717e43f4dda3cc6f2acdb084c26679`
- 提交说明：`feat: add 2.2.0 stage A booking data foundation`
- 阶段B开始前分支：`release/2.2.0`
- 阶段B开始前工作树：干净
- 阶段A复核基线：656/656通过

## 3. 阶段B提交

- 阶段B功能提交：`b54bd0d7d850f4811ce196094a78b0b18878ceaa`
- 提交说明：`feat(calendar): add scheduling calendar interface`
- 本报告编写时当前分支：`release/2.2.0`
- 本报告编写时HEAD：`b54bd0d7d850f4811ce196094a78b0b18878ceaa`
- 阶段B功能提交后工作树：干净
- 本报告作为独立回传文档在功能提交后生成，不改变阶段B功能检查点定义。

## 4. 产品、数据库和发布边界

- 产品版本：`2.2.0`
- 文件版本：`2.2.0.0`
- SchemaVersion：`2`
- 未修改Schema 1迁移
- 未修改Schema 2迁移
- 未新增数据库表
- Schema 2仍严格只有四张阶段A日历表：
  1. `ShootBookings`
  2. `ShootRequirementItems`
  3. `BookingDocuments`
  4. `BookingReminders`
- 未创建 `ProjectRelationships`
- 未实现多项目关系
- 未操作用户正式LocalAppData数据库
- 未注入Release演示排期

阶段B只在现有Schema 2上补充已归档排期的分页查询方法，并复用 `IsArchived`、`StartAtUtc` 和 `Id`；没有改变表、字段、索引、约束或迁移。

## 5. 页面和ViewModel

新增独立页面、子控件和ViewModel：

1. `WorkCalendarView` / `WorkCalendarViewModel`
2. `MonthCalendarView` / `MonthCalendarViewModel`
3. `WeekCalendarView` / `WeekCalendarViewModel`
4. `DayCalendarView` / `DayCalendarViewModel`
5. `DaySchedulePanel` / `DaySchedulePanelViewModel`
6. `ShootBookingDetailsView` / `ShootBookingDetailsViewModel`
7. `ShootBookingEditorView` / `ShootBookingEditorViewModel`
8. `ShootRequirementsPanel` / `ShootRequirementsViewModel`
9. `ArchivedBookingsView` / `ArchivedBookingsViewModel`

架构边界：

- `MainWindow.xaml`只增加左侧入口和独立页面宿主，没有把日历网格写入MainWindow。
- `MainViewModel`只持有 `WorkCalendarPage` 并协调 `WorkCalendar` 导航。
- `MainViewModel`不保存排期集合，不执行排期CRUD。
- 日历ViewModel只访问 `IShootBookingService` 和 `IProjectRepository`。
- 页面和ViewModel不直接访问SQLite。
- ViewModel不执行文件复制、移动或删除。
- 未建立第二套导航、通知、日志或错误码系统。

## 6. 导航入口

- 左侧一级入口新增“工作日历”。
- 导航键：`WorkCalendar`。
- 工作日历是独立页面宿主。
- 默认进入月视图。
- 当前MainWindow仍沿用既有导航体系，没有创建第二套导航服务。

## 7. 月视图

- 固定六周、42天网格。
- 周一作为每周第一列。
- 显示上月和下月补位日期。
- 当前日期有边框标识。
- 选中日期有独立背景标识。
- 每日最多显示3条排期。
- 超出后显示“另有N项”。
- 点击日期更新当天排期列表。
- 点击排期打开右侧详情抽屉。
- 双击空白日期按09:00至10:00创建排期。
- 点击补位日期会切换并重新查询对应月份。
- 月格不显示金额。
- 状态同时使用图形符号和文字，不只依赖颜色。

## 8. 周视图

- 七列时间轴。
- 每列包含全天/跨天区域。
- 每列包含24个小时时间槽。
- 点击任意时间槽以对应日期和整点创建排期。
- 普通排期按起始小时放入时间槽。
- 跨天排期放入全天/跨天区域。
- 支持垂直滚动。
- 高DPI或窄可用空间下支持水平滚动。

## 9. 日视图和当天列表

- 日视图包含单日全天/跨天区域。
- 日视图包含00:00至23:00的24个时间槽。
- 点击时间槽使用对应日期和整点创建排期。
- 右侧 `DaySchedulePanel` 显示选中日期的全部排期。
- 点击当天列表项目打开详情。
- 跨天排期会显示在其覆盖的每个当地日期。

## 10. 顶部工具栏和搜索

顶部工具栏提供：

- 今日
- 上一个周期
- 下一个周期
- 日期跳转
- 月/周/日切换
- 拍摄状态筛选
- 拍摄类型筛选
- 搜索框
- 搜索范围
- 新建排期

搜索范围：

1. 当前视图，默认
2. 全部未归档排期

当前视图查询：

- 月视图只读取当前42天网格范围。
- 周视图只读取当前7天。
- 日视图只读取当前日。
- 使用时间重叠查询，不加载全部历史。

全部未归档搜索：

- 默认每页50条。
- 服务层最大限制100条。
- 使用阶段A `(StartAtUtc, Id)` 游标分页。
- 提供“加载更多”。
- 不一次读取全部历史记录。
- 关键词、状态、类型或搜索范围变化时取消旧查询。
- “加载更多”也复用当前查询取消令牌，筛选变化后不会把旧页追加到新结果。
- 搜索词只作为参数化本地查询条件，不写入日志。

## 11. 新建和编辑

新建、编辑共用 `ShootBookingEditorView`，由独立模态窗口承载；窗口宽高根据工作区限制，不固定超过视口高度，内容独立滚动。

字段包括：

- 项目名称
- 客户显示名称
- 开始日期和时间
- 结束日期和时间
- 时区
- 全天
- 拍摄地点
- 拍摄类型
- 拍摄状态
- 具体拍摄要求
- 准备说明
- 准备清单
- 拍摄总金额
- 定金
- 已收金额
- 关联单一项目
- 允许重叠
- 内部备注

默认规则：

- 状态为待确定。
- 不允许重叠。
- 提醒仍保持阶段A默认关闭；阶段B编辑器不启动提醒。
- 月视图双击日期默认09:00至10:00。
- 周/日视图使用点击时间槽。
- 当选中日期为今天时，顶部新建使用下一个整点；23点后自然进入次日00:00。

保存验证：

- 必填字段
- 结束时间晚于开始时间
- 有效Windows时区
- 全天排期当地零点和右开结束日期
- 非负金额
- 时间冲突
- 金额非阻断警告

保存后只重新查询当前有限视图，并刷新详情；不读取全部历史。

## 12. 冲突交互

硬冲突显示：

- 冲突排期名称
- 客户显示名称
- 起止时间
- 地点
- 重叠时长
- 当前状态

提供三种明确决策：

1. 返回修改
2. 仍然保存
3. 标记当前排期允许重叠并保存

界面不会静默覆盖，不会自动修改时间。

已取消和已归档排期继续不参与阻断冲突；首尾相接继续不判定为冲突。

## 13. 金额显示和警告

- 日历格和搜索结果默认不显示金额。
- 详情默认用 `••••••` 遮罩金额。
- 用户主动点击后显示金额。
- 编辑器允许编辑总金额、定金和已收金额。
- UI金额输入换算为阶段A整数最小货币单位后保存。
- 已收金额高于总金额允许保存。
- UI明确显示“已收金额高于当前拍摄总金额”。
- 不自动修改总金额、定金或已收金额。
- 负余额显示为“多收金额”，并显示绝对金额。
- 未加入支付、发票、收入统计或在线财务能力。

## 14. 准备清单

支持：

- 新增
- 编辑
- 移除单个准备项
- 上移和下移排序
- 优先级
- 完成和取消完成
- 完成率

准备清单通过阶段A排期服务保存。归档排期不会删除准备清单。

## 15. 归档和恢复

阶段B只提供：

- 归档
- 查看已归档
- 恢复

归档行为：

- 从普通日历和全部未归档搜索隐藏。
- 通过阶段A事务禁用提醒。
- 保留准备清单。
- 保留文档关联。
- 保留金额和备注。
- 不删除任何电脑文件。

已归档页面：

- 使用现有Schema字段分页查询。
- 默认每页50条。
- 提供查看。
- 提供恢复。
- 不提供编辑。
- 不提供 `DeleteAsync`、`PurgeAsync` 或永久删除按钮。
- 恢复后提醒仍保持关闭。

## 16. DPI、主题和响应式布局

- 日历界面使用WPF逻辑DIP。
- 顶部工具栏使用WrapPanel，空间不足时换行。
- 周、日时间轴允许滚动。
- 详情抽屉和编辑器内容独立滚动。
- 编辑模态窗口受当前工作区宽高限制。
- 全部颜色使用现有动态主题资源。
- 支持既有深色、浅色和高对比资源切换机制。
- 状态使用符号和文字，不只依赖颜色。

自动化验收：

- 125%逻辑DPI等效视口测量通过。
- 150%逻辑DPI等效视口测量通过。
- 200%逻辑DPI等效视口测量通过。
- 隔离测试只在测试线程中实例化、测量和排列WPF控件，不显示到用户当前桌面。
- 未要求用户切换Windows缩放。
- 未要求用户截图。
- `physicalDpiManuallyTested=false`，作为已批准限制，不构成阶段B失败。

## 17. 键盘、焦点和无障碍

支持：

- `Ctrl+N` 新建
- `Ctrl+F` 聚焦搜索
- `Ctrl+T` 今日
- `Alt+1` 月视图
- `Alt+2` 周视图
- `Alt+3` 日视图
- `PageUp/PageDown` 切换周期
- `Enter` 激活有焦点的排期按钮并打开详情
- `Esc` 关闭详情或编辑窗口

无障碍：

- 页面、筛选、搜索、新建、时间槽、详情、归档和恢复等关键控件设置Automation名称。
- 关键入口补充HelpText。
- 排期按钮的自动化名称包含项目、客户、时间和状态。
- 关闭编辑窗口后焦点返回原控件。

## 18. UI隔离验收

- 使用独立测试线程和测试替身服务。
- 不启动正式应用数据库。
- 不显示测试窗口到用户桌面。
- 不接管用户输入。
- 不要求人工截图。
- 测试结束后关闭测试Application对象。
- 未执行安装版Win32交互，因为本阶段明确禁止生成安装包；该项不作为阶段B硬门禁。

## 19. 测试结果

Debug最终测试：

- Core/业务/架构：642/642
- WPF：29/29
- DPI：27/27
- 合计：698/698
- 失败：0
- 跳过：0

Release最终测试：

- Core/业务/架构：642/642
- WPF：29/29
- DPI：27/27
- 合计：698/698
- 失败：0
- 跳过：0

阶段A基线656项全部保留并通过；阶段B新增42项验收。

新增覆盖包括：

- 工作日历导航
- 默认月视图
- 月/周/日切换结构
- 今日和日期范围
- 42天月网格
- 每日3项和溢出
- 七列周视图和24小时时间槽
- 单日24小时时间槽
- 跨天显示
- 当前视图搜索
- 状态和类型筛选
- 全部未归档50条游标分页
- 加载更多
- 取消旧搜索
- 已归档50条游标分页
- 归档和恢复
- 无永久删除入口
- 金额默认遮罩
- 超额金额警告和多收金额
- 准备清单增删改排序和完成率
- 冲突三选项和冲突状态
- ViewModel不直接访问SQLite
- MainViewModel不承担日历CRUD
- Release不注入演示排期
- 主题动态资源
- 键盘和无障碍名称
- 125%、150%、200%隔离逻辑DPI视口测量

## 20. 构建结果

- Debug构建通过。
- Release构建通过。
- Debug：0警告、0错误。
- Release：0警告、0错误。
- 未执行Publish。
- 未生成安装包。

## 21. 本阶段未实现范围

严格未实现：

- `BookingDocumentsPanel` 正式文档界面
- 文档拖入、复制和重新定位界面
- 提醒调度
- 错过提醒补报
- 工作台今日拍摄
- 工作台未来7天
- `ProjectRelationships`
- 多项目关系
- 永久删除
- 项目模板
- 项目状态机
- 项目健康检查
- 本地选片
- 精修片回匹配
- 联系表
- 交付包
- 文件夹监听
- CRM
- 在线预约
- 支付
- 云同步

## 22. Git和发布状态

- 当前开发分支：`release/2.2.0`
- 阶段A检查点：`49246dbdb5717e43f4dda3cc6f2acdb084c26679`
- 阶段B功能检查点：`b54bd0d7d850f4811ce196094a78b0b18878ceaa`
- 未合并main
- 未创建 `v2.2.0` Tag
- 未生成安装包
- 未自动进入阶段C

## 23. 是否建议进入阶段C

阶段B功能、边界审计、Debug/Release构建和698项测试均已通过，因此技术上建议在用户审查本报告并明确授权后进入阶段C。

本报告不构成自动进入阶段C的授权；当前任务在阶段B完成后立即停止。

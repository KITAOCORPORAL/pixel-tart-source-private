# 像素蛋挞 2.0.1 安装版人工 UI 验收报告

## 一、结论

本轮完成了当前唯一有效 2.0.1 安装包的哈希校验、隔离安装、真实安装进程启动和可执行的人工交互检查，但**未完成媒体证据交付**，因此本轮人工 UI 验收不能判定为通过。

没有使用 UI Review 渲染器、设计稿、网页重绘、Photoshop 拼图、旧截图复制或同图重命名。Windows 图形捕获链路持续拒绝取帧或只返回白屏/桌面背景，所有无效测试图片均已删除，没有用无效画面冒充验收证据。

同时发现当前安装版存在两个需要复核的问题：

1. 左侧“设置”入口通过按钮调用和坐标点击均无响应，未打开设置页或设置窗口；
2. 工具箱 Popup 底部“查看全部工具”通过按钮调用和坐标点击均未跳转；完整工具箱页面可通过顶部“工具”菜单正常打开。该问题建议再用物理鼠标复核一次。

基于媒体证据缺失和设置入口无响应，本报告**不建议直接进入下一阶段开发或对外发布**，建议先完成缺陷复核和一次可正常录屏的本机验收。

## 二、安装包与实际安装

- 安装包：`D:\AI AGENT\RAWSelectionAssistant\artifacts\installer\像素蛋挞_Setup_2.0.1_x64.exe`
- 文件大小：48,765,008 字节
- SHA-256：`5255D10DCAAE6E5EA4E7916E62AD07B0C38487BA86EBF73BC8DC5C0FCE00241C`
- 哈希是否与指定值一致：是
- 隔离安装路径：`D:\AI AGENT\RAWSelectionAssistant\artifacts\final-manual-review\2.0.1\installed-app`
- 实际启动文件：`D:\AI AGENT\RAWSelectionAssistant\artifacts\final-manual-review\2.0.1\installed-app\KitaoPhotoSelector.exe`
- 实际启动版本：2.0.1
- 窗口标题：像素蛋挞
- 隔离卸载结果：成功，退出码 0
- 环境恢复：原用户数据和原 2.0.0 安装记录均已恢复

本轮没有修改产品源码，没有重新构建产品，没有重新生成、覆盖或改名安装包，没有提升版本号。

## 三、预期截图与实际状态

输出目录：`D:\AI AGENT\RAWSelectionAssistant\artifacts\final-manual-review\2.0.1`

| 文件 | 对应状态 | 结果 |
| --- | --- | --- |
| `01_Installed_Workbench_Dark.png` | 安装版深色普通工作台 | 未生成：图形捕获阻塞 |
| `02_Installed_Toolbox_Popup.png` | 工具箱 Popup | 未生成；交互结构已实测 |
| `03_Installed_Toolbox_FullPage.png` | 完整工具箱页面 | 未生成；页面结构已实测 |
| `04_Installed_RecentProjects_WithData.png` | 最近项目有数据 | 未生成；数据状态已实测 |
| `05_Installed_CompletedProjects_Empty.png` | 已完成项目空状态 | 未生成；空状态已实测 |
| `06_Installed_TaskCenter_WithTasks.png` | 任务中心有任务 | 未生成；没有伪造长耗时任务 |
| `07_Installed_TaskCenter_Empty.png` | 任务中心空状态 | 未生成；空状态已实测 |
| `08_Installed_Settings_Dark.png` | 深色设置窗口 | 未生成；设置入口无响应 |
| `09_Installed_Workbench_Light.png` | 浅色工作台 | 未生成；主题状态已实测 |
| `10_Installed_Compact_1280.png` | 1280 宽状态 | 未生成；响应式结构已实测 |
| `11_Installed_Sidebar_Collapsed.png` | 侧栏折叠 | 未生成；折叠结构已实测 |
| `12_Installed_Feedback_Dialog.png` | 建议与问题反馈 | 未生成；窗口内容已实测 |

因此不能回答“每张截图是否为不同内容”为“是”。当前没有 12 张可供哈希对比的有效截图。对应清单：

- `D:\AI AGENT\RAWSelectionAssistant\artifacts\final-manual-review\2.0.1\UI截图_SHA256.csv`
- `D:\AI AGENT\RAWSelectionAssistant\artifacts\final-manual-review\2.0.1\UI截图_SHA256.json`

两个清单均将 12 项标记为 `NOT_GENERATED`，SHA-256 留空，不使用重复哈希冒充独立状态。

## 四、录屏与总览

- 预期录屏：`D:\AI AGENT\RAWSelectionAssistant\artifacts\final-manual-review\2.0.1\像素蛋挞_2.0.1_安装版最终交互验收.mp4`
- 实际状态：未生成
- 预期总览：`D:\AI AGENT\RAWSelectionAssistant\artifacts\final-manual-review\2.0.1\像素蛋挞_2.0.1_安装版UI验收总览.png`
- 实际状态：未生成

Windows 文件名不能包含 `*`，所以如后续补录，用户要求中的星号将使用下划线替代。

## 五、已完成的实际交互检查

### 1. 工具箱 Popup

- 顶部工具箱快捷入口能够打开 Popup；
- Popup 显示“本地工具箱”和“8 项”；
- 工具排列为 2 列 × 4 行；
- 8 项分别为本地分片、批量压缩、批量水印、删废片、FTP 工具、照片整理、批量重命名、批量转档；
- Escape 能关闭 Popup；
- Popup 底部“查看全部工具”未能通过按钮调用或坐标点击跳转，属于疑似交互缺陷；
- 受媒体捕获阻塞影响，无法通过截图核准 Popup 锚点和阴影位置；
- 点击外部关闭没有形成可靠复测结论。

### 2. 完整工具箱页面

- 可从顶部“工具”菜单进入；
- 页面标题为“工具箱”；
- 页面说明为像素蛋挞本地照片工具；
- 工具卡以三列结构出现；
- 页面本身可访问，说明 Popup 底部跳转问题不等同于页面缺失。

### 3. 最近项目与空状态

- “最近项目”状态真实显示 `[演示] 海边婚礼精选`；
- 项目显示 `92 个文件`；
- 切换“已完成”后真实显示“暂无已完成项目”；
- 两个状态的可访问性内容明确不同，已解决“数据状态与空状态无法区分”的逻辑核验缺口；
- 因图形捕获阻塞，未能补齐两张不同像素内容的 PNG 证据。

### 4. 任务中心

- 1600×920 状态下右侧任务中心存在；
- 空状态显示“暂无待处理任务”；
- 1280 逻辑宽度下任务中心折叠为顶部“任务中心”按钮；
- 点击后抽屉能够展开，抽屉包含当前任务、等待确认、冲突文件、未找到文件和任务历史入口；
- 抽屉存在“收起任务中心”操作，关闭逻辑正常；
- 未获得可控的长耗时真实任务，因此没有声称“有任务状态”通过，也没有注入假任务。

### 5. 侧栏

- 展开宽度约 241 物理像素（150% DPI）；
- 折叠宽度约 64 物理像素；
- 折叠后工作台、本地分片、归片工作区、项目历史、授权与版本、设置、帮助、工具箱、使用教程、反馈入口均保留；
- 折叠后只保留图标，主内容获得释放宽度；
- 展开后可访问性名称完整，没有文字重叠；
- 高 DPI 下元素边界仍完整。

### 6. 1280 宽度

- 将窗口切到 1280×720 逻辑尺寸后，实际自动化边界为 1920×1080（系统为 150% DPI）；
- 侧栏保持图标模式；
- 右侧任务中心折叠为顶部按钮；
- 顶部主入口和四个快捷入口仍存在；
- 项目概览、处理任务和项目筛选区域没有控件边界重叠；
- 此结论来自真实可访问性边界检查，缺少像素截图佐证。

### 7. 深浅主题

- 默认深色工作台正常启动；
- “视图 → 浅色模式”命令执行后，当前安装版设置写入 `Theme=1`；
- “视图 → 深色模式”命令执行后，当前安装版设置恢复为 `Theme=2`；
- 两次状态切换均即时生效并可追溯；
- 左侧“设置”入口无响应，所以无法打开“深色设置窗口”进行页面验收。

### 8. 反馈窗口

- “建议与问题反馈”窗口能够正常打开；
- 作者邮箱显示为 `3183483929@qq.com`；
- 包含“复制邮箱”“撰写邮件”“关闭”按钮；
- 点击“复制邮箱”后状态文字变为“邮箱已复制”；
- 没有触发“撰写邮件”，避免未经请求打开外部邮件程序；
- 窗口明确说明邮件模板不会附带照片、项目、授权或日志信息。

### 9. 顶部菜单

- 文件、项目、编辑、视图、工具、帮助六个菜单均存在且文字完整；
- 深色模式下菜单及“视图”子菜单的可访问性结构完整；
- 受媒体捕获阻塞影响，色彩对比度只能从运行状态观察，无法提供像素证据。

## 六、发现的问题

1. **阻断：设置入口无响应。** 左侧设置按钮使用调用和坐标点击均未打开设置页/窗口。需要在下一轮修复或物理鼠标复核。
2. **疑似缺陷：Popup 的“查看全部工具”不跳转。** 顶部工具菜单可以打开完整工具箱，因此更可能是 Popup 底部按钮绑定或 Popup 点击处理问题。
3. **环境阻断：系统无法生成可信截图和录屏。** 不是使用旧图可以规避的问题。
4. **证据缺口：任务中心有任务状态未验证。** 没有创建假任务，需在可录屏环境用真实扫描/复制任务补测。
5. **未确认：Popup 点击外部关闭。** Escape 已确认，外部点击需要物理鼠标再测。

## 七、媒体捕获阻塞说明

以下只读捕获方式均已尝试：

- Computer Use 的 Windows.Graphics.Capture：`SetIsBorderRequired` 返回 `0x80004002`；
- `Graphics.CopyFromScreen`：返回“句柄无效”；
- `PrintWindow` 标志 0/1/2/3：只得到白色客户区；
- 自建 Windows.Graphics.Capture 窗口捕获：只得到白色客户区；
- 显示器捕获：只得到桌面背景或超时；
- Windows 截图工具：报告当前未运行，要求重启计算机；
- FFmpeg `gdigrab`：`Failed to capture image (error 5)`；
- 同桌面交互取证窗：`CopyFromScreen` 仍返回句柄无效；
- Xbox Game Bar：当前系统未提供可用包。

诊断文件：`D:\AI AGENT\RAWSelectionAssistant\artifacts\final-manual-review\2.0.1\媒体取证阻塞诊断.json`

## 八、是否建议进入下一阶段

当前不建议直接进入下一阶段或对外发布。建议先完成：

1. 修复或确认“设置”入口；
2. 修复或确认 Popup 的“查看全部工具”；
3. 在重启后的可正常录屏 Windows 会话中，用物理鼠标重做 12 个独立状态；
4. 用真实长耗时任务补测任务中心有任务状态；
5. 生成 12 张不同 SHA-256 的 PNG、60–120 秒 MP4 和总览图后，再签署人工 UI 验收通过。

## 九、恢复与完整性

- 隔离安装副本已卸载；
- 原用户数据已恢复；
- 原 2.0.0 安装记录已恢复；
- 安装包仍存在；
- 安装包大小仍为 48,765,008 字节；
- 安装包 SHA-256 仍为 `5255D10DCAAE6E5EA4E7916E62AD07B0C38487BA86EBF73BC8DC5C0FCE00241C`；
- 产品源码未修改；
- 未生成新安装包；
- 版本号未变化。

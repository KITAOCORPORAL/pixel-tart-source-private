<view class="page"><view class="title">{{selectedCount}} / {{targetCount}}</view><view class="photo-grid"><view class="photo-card" wx:for="{{assets}}" wx:key="selectionAssetId"><image src="{{item.thumbUrl}}" mode="aspectFill"/><view class="photo-meta">{{item.originalFileName}}<button size="mini" data-id="{{item.selectionAssetId}}" bindtap="cancel" disabled="{{confirmed}}">取消</button></view></view></view><button class="button" bindtap="confirm" disabled="{{confirmed}}">确认选片</button></view>

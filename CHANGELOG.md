# 更新日志

本项目从 2.0.3 开始采用本地 Git 基线、不可变版本产物目录和发布清单管理。

## [2.0.3] - 2026-07-31

- 冻结“像素蛋挞”2.0.3 当前源码、测试和开发蓝图，作为后续维护版本的基线。
- 当前自动化测试：405 / 405 通过。
- 当前安装包：`像素蛋挞_Setup_2.0.3_x64.exe`。
- 安装包大小：48,754,028 字节。
- 安装包 SHA-256：`D6768A5DDC8A3CBCD5AA9C9FB21675006252A7FE480DA9BEF1A9AC60FD8F1AA9`。
- 授权 Provider 保持 `None`，Release 不启用 Mock 授权。

## [2.0.3.1] - 2026-07-31

- 基线冻结与 UI 验收缺口修复。

## [2.1.0] - 2026-08-04

- 新增 SQLite 本地项目数据库，当前 `SchemaVersion=1`。
- 新增顺序迁移、迁移前备份、恢复保护与 2.0.4 旧 JSON 保留导入。
- 新增持久化统一任务引擎和明确的任务状态机。
- 支持暂停、继续、取消和受限重试，并只在安全边界切换状态。
- 新增 `NeedsAttention`，冲突和高风险情形等待用户明确决定。
- 新增 `PartiallyCompleted`，分别记录成功、失败、跳过和等待确认项。
- 新增崩溃恢复，异常中断任务恢复为 `Interrupted` 并保留检查点。
- 新增 `FileOperationPlan`、执行前验证、`CreateNew` 安全写入和校验后移动。
- 新增 `UndoJournal`，撤销前验证文件所有权、大小、哈希和路径占用。
- 新增 `NotificationCenter`，统一长任务、部分失败、冲突和空状态通知。
- 新增 `ErrorCodeCatalog`，统一文件、图像、任务、数据库和网络预留错误码。
- 新增 `AuditLog`，记录任务、迁移、恢复与文件操作并对敏感路径脱敏。
- 将扫描、索引、归片复制、整理图片、拼图导出和报告生成接入全局任务中心。
- 工作日历仅预留本地提醒、提醒仓储和项目关系接口；未实现页面、排期业务表或后台提醒。

## [2.0.4] - 2026-08-03

- 快捷工具支持拖拽、键盘、右键和管理窗口排序。
- 整理图片支持真实扫描、分组、清单、安全复制/移动、撤销和报告。
- 拼图支持数据驱动模板、长图模式、JPG/PNG 安全导出。

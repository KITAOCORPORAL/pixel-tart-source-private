# 北尾鸲归片助手 1.4.0 UI 页面验收清单

## 全局框架

- [x] 版本显示为 1.4.0。
- [x] 顶层菜单顺序为文件、项目、编辑、视图、工具、帮助。
- [x] 菜单命令复用原 ViewModel，不复制业务逻辑。
- [x] 应用栏显示项目、状态、当前步骤、索引、版本与取消任务。
- [x] 侧栏支持 220 / 0 两种宽度并持久化；收起后侧栏完全退出布局。
- [x] 底部状态和进度保持可见。
- [x] 窗口最小尺寸下主内容仍可滚动。

## 项目中心

- [x] 新建项目与继续最近项目为首要入口。
- [x] 最近项目卡片保留名称、更新时间、摘要和继续操作。
- [x] 免费版与专业版状态可见。

## 归片工作区

- [x] 四步流程始终可见并可点击切换。
- [x] 教程要求的命名控件全部保留。
- [x] 来源目录、目录用途、排序和扫描保持原命令。
- [x] 客户 JPG 安全文案完整显示。
- [x] 三种客户 JPG 处理模式保持原逻辑。
- [x] 匹配、复制、报告、打开目录、保存和清空保持原命令。

## 结果表

- [x] 支持按输入、标准名、数字编号和备注搜索。
- [x] 支持只看冲突与待处理。
- [x] `查看明细` 位于总体状态右侧，无需向后滚动。
- [x] `查看明细`、下方完整明细按钮和 `Alt+Enter` 指向同一命令。
- [x] 默认选中第一条匹配记录，快速摘要同步更新。
- [x] 表格支持横向和纵向滚动。
- [x] 明细窗口支持 Esc 关闭，候选窗口支持 Enter 确认、Esc 取消。

## 设置与主题

- [x] 浅色主题完成。
- [x] 深色主题完成。
- [x] 跟随 Windows 主题完成。
- [x] Windows 高对比度资源完成。
- [x] 强调色预设、自定义色与可读前景完成。
- [x] 舒适 / 紧凑密度完成。
- [x] 侧栏三种行为完成。
- [x] 标准 / 减少动效设置完成。
- [x] 标准 / 大字号完成。
- [x] 实时预览、自动保存和恢复默认完成。
- [x] 主题切换不重置 ViewModel 或当前项目。

## 其他页面

- [x] 项目历史使用统一表格样式。
- [x] 授权与版本页不泄露 Token、私钥或完整激活码。
- [x] Provider=None 时显示生产授权未配置的友好说明。
- [x] 帮助页显示 1.4.0 和 22 步教程入口。
- [x] 帮助菜单提供建议与问题反馈，支持复制邮箱和由用户主动撰写邮件。
- [x] `Ctrl+B` 切换侧栏，`Ctrl+Shift+F` 打开反馈窗口。
- [x] 教程遮罩、卡片、错误状态跟随主题。

## 发布安全

- [x] `OutputType=WinExe`，无控制台窗口。
- [x] `win-x64` self-contained。
- [x] `Provider=None` 保持不变。
- [x] `allowMockProvider=false` 保持不变。
- [x] Release XAML 无 UI Preview 入口。
- [x] 原有测试与新增 UI 测试全部通过。

## 实机验收记录

- [x] 隔离验收版正常启动，辅助功能树识别 1.4.0、六个顶层菜单和主要页面。
- [x] 通过“视图”菜单实际切换深色，页面摘要显示“当前主题：深色”，设置持久化。
- [x] 通过“视图”菜单实际切换浅色，页面摘要显示“当前主题：浅色”，设置持久化。
- [x] 侧栏收起状态写入隔离设置并在下一次启动恢复。
- [x] 教程第 12 步可识别三条“查看明细”，按钮位于总体状态右侧。
- [x] 独立验收安装包退出码为 0，安装到 `C:\Program Files\北尾鸲归片助手_验收测试`。
- [x] 安装后的 EXE 为 1.4.0，正常启动并显示新菜单、应用栏与工作区。
- [x] 安装目录授权配置仍为 `Provider=None`。
- [ ] 本机 Windows.Graphics.Capture 返回 `0x80004002`，无法生成自动截图；可访问性结构验收不受影响。

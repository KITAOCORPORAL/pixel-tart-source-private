# Pixel Tart Studio UI — 分段选择

PixelTart.Segmented 使用单选 ListBox 与可访问 ListBoxItem，保留 SelectionItem、方向键与焦点。选中为弱面加 Accent 底边。四个参考视图：原片／仿色结果／左右对比／并排对比；窄宽允许换行，不能裁动作。

实施资源：`src/RAWSelectionAssistant/Resources/DesignSystem/Studio.Controls.xaml`、`Studio.Symbols.xaml` 及现有兼容字典。
状态：v1 实施中，用户确认待完成。全应用推广为 PARTIAL；本文件不是验收 PASS。

# Pixel Tart Studio UI — 可访问性

保留 AutomationId/Name、Invoke、SelectionItem、Value、ExpandCollapse、Enter/Space/Esc/Shift+F10。焦点使用 Accent 边线；图标不代替名称。逻辑缩放与物理 DPI 分开报告，原生文件对话框契约仍属安装验收。

实施资源：`src/RAWSelectionAssistant/Resources/DesignSystem/Studio.Controls.xaml`、`Studio.Symbols.xaml` 及现有兼容字典。
状态：v1 实施中，用户确认待完成。全应用推广为 PARTIAL；本文件不是验收 PASS。

# Pixel Tart Studio UI — 输入

继承已验证的暗色 TextBox、ComboBox、Calendar/DatePicker 模板，不替换原生 Open/Save。输入对比用背景与焦点，不使用白面；保留 PART_TextBox、PART_Popup、Value 和 ExpandCollapse。

实施资源：`src/RAWSelectionAssistant/Resources/DesignSystem/Studio.Controls.xaml`、`Studio.Symbols.xaml` 及现有兼容字典。
状态：v1 实施中，用户确认待完成。全应用推广为 PARTIAL；本文件不是验收 PASS。

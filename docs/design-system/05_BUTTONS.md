# Pixel Tart Studio UI — 按钮

PixelTart.Button.Primary / Secondary / Ghost / Icon / Danger。普通按钮最小高 34 DIP，不固定文字宽；图标按钮 30×30、图形 18。保留 Normal/Hover/Pressed/Disabled/Focus。Selected 使用可选择控件而非假按钮状态。图标按钮必须有中文 AutomationName 和 Tooltip。

实施资源：`src/RAWSelectionAssistant/Resources/DesignSystem/Studio.Controls.xaml`、`Studio.Symbols.xaml` 及现有兼容字典。
状态：v1 实施中，用户确认待完成。全应用推广为 PARTIAL；本文件不是验收 PASS。

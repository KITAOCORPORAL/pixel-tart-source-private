# Pixel Tart Studio UI — 布局

全局中文导航不变；复杂工作区为上下文栏加大内容区。策划栏 280 DIP、七模块保留。参考仿色大主图加 360 DIP 检查器；核心参数常显，高级默认收起。图库搜索最小 300，窄宽保留工具栏滚动。1/2/3/4+ Hero 使用单图／等宽／左大右双／主图加次图，Uniform 不裁构图。

实施资源：`src/RAWSelectionAssistant/Resources/DesignSystem/Studio.Controls.xaml`、`Studio.Symbols.xaml` 及现有兼容字典。
状态：v1 实施中，用户确认待完成。全应用推广为 PARTIAL；本文件不是验收 PASS。

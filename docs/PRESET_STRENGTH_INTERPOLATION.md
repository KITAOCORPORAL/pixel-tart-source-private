# Preset strength interpolation

Stage 4 is **NOT STARTED**. The existing LUT preview strength path is not a general third-party preset strength implementation. Future preset nodes must interpolate parameter space where semantics are known, use look-space interpolation for LUTs and curves where necessary, and prove 0% identity and 100% full-preset identity with a single undo transaction for a drag.

# 策划中心最终截图自审

日期：2026-09-20。PRODUCT_SOURCE_SHA：`8cb95e6d2c4814717dc31a8c6aa19f8b2c89c9cc`。

目录：`artifacts/planning-proposal-final/screenshots/`。逐张读取最终 PNG 后记录；不是生成成功即通过。
真实 App.xaml、MainWindow、生产服务组合；隔离合成数据。图中参考图为测试图形，不是摄影作品。
截图方式为 WPF 客户区视觉渲染并映射到 1920×1080；不是操作系统桌面截图，也不是安装版截图。

| 文件 | 图片/留白/对齐/密度 | 圆角/控件/表单感 | 阅读/截断/WPF 外观 | 判定 |
|---|---|---|---|---|
| 01_文字 | 主视觉大、次图小，正文限宽，首屏主次清楚 | 仅按需编辑，无表单墙 | 大标题/副标题/正文层级成立，无可见截字 | 当前静态视图 PASS |
| 02_参考图 | 原比例，四列，图片为主，标签轻量 | 顶部两个入口，未用大卡片边框 | 标签自然，摄影构图未裁切 | 当前静态视图 PASS |
| 03_情绪板 | 图片摘要与分组标题，合理留白 | 自由排布入口不复制画布 | 下方关联摘要需滚动，无横向溢出 | 当前静态视图 PASS |
| 04_镜头清单 | 六行左右首屏，编号/名称/场景层级清楚 | 一条轻分隔，不是 DataGrid | 少量英文为测试场景内容，中文状态清晰 | 当前静态视图 PASS |
| 05_灯光图 | 图片优先，镜头关系为次级文字 | 无模拟器/表单堆叠 | 横竖图保持比例，信息不挤压图片 | 当前静态视图 PASS |
| 06_服化道 | 服装、妆发顺序正确，组间留白 | 无常驻编辑输入框 | 旧未分组参考保留在具名组之后 | 当前静态视图 PASS |
| 07_文件 | 单附件，空白较多但不填无用卡片 | 打开/定位/移除三个必要操作 | 来源、类型、时间清楚；实际系统打开未验收 | 静态 PASS，外部操作待测 |
| 08_预览模式 | 主视觉及正文读物布局 | 列表、顶栏操作、七模块编辑导航隐藏 | 无 TextBox 残留，保留轻量 Esc 提示 | 当前静态视图 PASS |
| 09_镜头详情抽屉 | 360 DIP，按需覆盖右侧，独立滚动 | 圆角/深色层次统一；表单只在详情出现 | 中文字段，可见区域无重叠 | 当前静态视图 PASS |
| 10_参考图右键菜单 | 底图正常，但目标菜单未完整出现在 PNG 内 | 不能从此图证明菜单圆角、图标、Hover | 不能视为菜单视觉验收证据 | INCOMPLETE，需重采 |
| 11_创建策划 | 居中、遮罩、对齐规整 | 统一圆角，少量必要表单，向量日历图标 | 有焦点边框；日期弹出日历未截图 | 当前静态视图 PASS，日历弹层待测 |
| 12_1080p | 两栏，940 DIP 正文，无永久 Inspector | 少量顶栏操作 | 七模块单行完整 | 软件布局 PASS |
| 13_125dpi | 主图可见，更多内容纵向滚动 | 保持两栏 | 导航单行；全局底部导航需滚动 | 软件布局 PASS |
| 14_150dpi | 正文区域收窄，主图部分在折叠线下 | 无新增侧栏 | 标题可读、七模块完整 | 软件布局 PASS |
| 15_200dpi | 首屏以标题为主，图需向下滚动 | 顶栏动作自动第二行 | 七模块紧凑单行，未见裁字；不等于物理 DPI 通过 | 软件布局 PASS |

## 仍需补证据

- Hover、Disabled、子菜单、Quick Preview 切图、Booking Picker、Export Dialog 及其各 DPI 弹层没有完整桌面截图矩阵；不宣称完整视觉闭环。
- 100/125/150/200 使用 LayoutTransform 软件布局检查，物理显示器 DPI = NOT TESTED。
- 真实摄影师首次使用 = PENDING USER；不虚构用户反馈。
- Windows Computer Use 截图失败 `SetIsBorderRequired failed: 不支持此接口 (0x80004002)`；点击失败 `coordinate input geometry is unavailable`。重新绑定仍失败，键盘 Tab 未改变焦点。安装版交互验收 BLOCKED。

## PDF 视觉复核

`策划案-验收.pdf`：23 页，5,617,473 bytes，300 DPI RASTER；各页整页图 2482×3510，提取文本 0 字符。
使用 Poppler 渲染全部 23 页，检查三张逐页联系表及首张 192 DPI 渲染（96 DPI 屏幕基准的 200%）。无空白页，页码连续，图像原比例，标题层级明确。
按 PDF 技能逐页检查发现深色 App 主题影响图片说明颜色，已在冻结前修成显式黑字白底并重新导出。多页只有两张图，留白较多；避免放大小预览制造细节。
这是合成图片的技术/排版检查，未证明真实高细节摄影图的最终印刷质量。安装版 UI 导出文件尚未生成，不用此文件冒充安装版导出。

You are resuming Pixel Tart development on a different Windows PC. First confirm the current directory is the `pixel-tart-source-private` Git repository. If not, locate it or clone `KITAOCORPORAL/pixel-tart-source-private` from GitHub after private-repository access is authorized; do not put credentials in the repository or URL. In the repository, run `git fetch --all --prune`, `git checkout integration/pixel-tart-developer-preview`, and `git pull --ff-only`. Confirm `git rev-parse HEAD` equals `git rev-parse origin/integration/pixel-tart-developer-preview`; stop and diagnose if not.

Read `docs/PIXEL_TART_HOME_DEV_HANDOFF.md`, `docs/COLOR_STUDIO_PHASE1_CLOSURE.md`, `docs/COLOR_STUDIO_CURRENT_IMPLEMENTATION_MAP.md`, and the latest 20 commits. Then run `powershell -ExecutionPolicy Bypass -File .\scripts\bootstrap-home.ps1` and `powershell -ExecutionPolicy Bypass -File .\scripts\verify-home-dev.ps1`. The new `scripts/verify-color-studio-native-capture.ps1` validates real Production WPF pixels through PrintWindow/BitBlt fallbacks and reached 100/100 stable frames on SHA `d00df6a`; its first 18-case visual drag run still has intermittent observer timeouts/order mismatches, so continue the synchronized capture diagnosis and remain BLOCKED until a clean run. The existing hook/fixture screenshots are not native pointer proof. Phase 2 core files provide bounded OKLab proxy/cache, camera state, and framework-neutral sample/cluster linking; production WPF visualization remains gated by Phase 1 closure.

Do not re-plan Pixel Tart, redo Photography or completed Color Studio Phase 1 features, revisit historical RC12, enter Phase 2 before formal Phase 1 closure, or install WSL, Ubuntu, Docker, or a VM. Preserve the shared Simple/Professional state, per-target isolation, existing renderer, scheme migration, cancellation/revision/last-wins behavior, and Pixel Tart palette. Physical display DPI remains a separate release-hardware gate; do not conflate it with the native interaction gate.

Latest staged professional pipeline audit: V4 has CPU foundation and synthetic 12/24/45/60MP timings, but no validated GPU backend or CPU/GPU parity. LibRaw production conversion is currently 8-bit sRGB. A standalone TIFF 8/16-bit writer exists for RGB buffers, but it does not preserve RAW high bit depth and has not been wired into production export. Vendor RAW fixture verification, high-bit-depth RAW-to-TIFF and GPU device/smoke/parity remain release blockers. Third-party preset import and hover are NOT STARTED. Read the current handoff and the new RAW/TIFF/GPU decision documents before continuing.

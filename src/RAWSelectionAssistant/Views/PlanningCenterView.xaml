<UserControl x:Class="RAWSelectionAssistant.Views.PlanningCenterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:RAWSelectionAssistant.Views"
             mc:Ignorable="d" d:DesignWidth="1600" d:DesignHeight="900"
             Background="{DynamicResource ContentBackgroundBrush}" Focusable="True"
             PreviewKeyDown="View_PreviewKeyDown" AutomationProperties.Name="项目策划工作区">
    <UserControl.Resources><BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/><local:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/><local:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/><Style x:Key="CompactExpander" TargetType="Expander"><Setter Property="IsExpanded" Value="False"/><Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/><Setter Property="Margin" Value="0,12,0,0"/></Style><Style x:Key="FilterToggleButton" TargetType="ToggleButton"><Setter Property="FontFamily" Value="{StaticResource ChineseFontFamily}"/><Setter Property="FontSize" Value="{DynamicResource BodyFontSize}"/><Setter Property="MinHeight" Value="{DynamicResource ControlHeight}"/><Setter Property="Padding" Value="14,8"/><Setter Property="Background" Value="Transparent"/><Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/><Setter Property="BorderBrush" Value="Transparent"/><Setter Property="BorderThickness" Value="1"/><Setter Property="Cursor" Value="Hand"/><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ToggleButton"><Border x:Name="Surface" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="{DynamicResource RadiusControl}" Padding="{TemplateBinding Padding}"><ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center" RecognizesAccessKey="True"/></Border><ControlTemplate.Triggers><Trigger Property="IsEnabled" Value="False"><Setter TargetName="Surface" Property="Opacity" Value="0.46"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter><Style.Triggers><Trigger Property="IsChecked" Value="True"><Setter Property="Background" Value="{DynamicResource DropdownSelectedBrush}"/><Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/></Trigger><Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="{DynamicResource DropdownHoverBrush}"/></Trigger><Trigger Property="IsKeyboardFocused" Value="True"><Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/><Setter Property="BorderThickness" Value="2"/></Trigger></Style.Triggers></Style></UserControl.Resources>
    <Grid>
    <Grid Visibility="{Binding IsOverview,Converter={StaticResource InverseBooleanToVisibilityConverter}}">
        <Grid.RowDefinitions><RowDefinition Height="76"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <Border BorderBrush="{DynamicResource DividerBrush}" BorderThickness="0,0,0,1" Padding="20,12">
            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <StackPanel><TextBlock Text="{Binding ProjectName}" Style="{StaticResource PageTitleText}"/><WrapPanel Margin="0,5,0,0"><TextBlock Text="{Binding BookingDate}" Style="{StaticResource CaptionText}"/><TextBlock Text="  ·  " Style="{StaticResource CaptionText}"/><TextBlock Text="{Binding Location}" Style="{StaticResource CaptionText}"/><TextBlock Text="  ·  " Style="{StaticResource CaptionText}"/><TextBlock Text="{Binding ProgressText}" Style="{StaticResource CaptionText}"/><TextBlock Text="  ·  " Style="{StaticResource CaptionText}"/><TextBlock Text="{Binding EstimatedTimeText}" Style="{StaticResource CaptionText}"/><TextBlock Text="  ·  " Style="{StaticResource CaptionText}"/><TextBlock Text="{Binding SaveStatus}" Style="{StaticResource CaptionText}"/></WrapPanel></StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center"><Button Content="全部策划" AutomationProperties.Name="返回策划总览" Command="{Binding ShowOverviewCommand}" Style="{StaticResource GhostButton}" Margin="0,0,8,0"/><Button Content="进入联机拍摄" Style="{StaticResource PrimaryButton}" Command="{Binding EnterTetherCommand}" AutomationProperties.Name="进入当前项目联机拍摄"/><Button Content="更多" Style="{StaticResource GhostButton}" Margin="8,0,0,0" ToolTip="更多项目操作" AutomationProperties.Name="更多项目操作"><Button.ContextMenu><ContextMenu><MenuItem Header="新建拍摄" Command="{Binding NewShotCommand}"/><MenuItem Header="复制当前拍摄" Command="{Binding DuplicateShotCommand}"/><Separator/><MenuItem Header="移除当前策划" Command="{Binding ArchiveShotCommand}"/></ContextMenu></Button.ContextMenu></Button></StackPanel>
            </Grid>
        </Border>
        <Grid Grid.Row="1" Margin="12"><Grid.ColumnDefinitions><ColumnDefinition x:Name="ShotColumn" Width="260" MinWidth="210" MaxWidth="310"/><ColumnDefinition Width="7"/><ColumnDefinition Width="*" MinWidth="520"/><ColumnDefinition Width="7"/><ColumnDefinition x:Name="InspectorColumn" Width="320" MinWidth="280" MaxWidth="380"/></Grid.ColumnDefinitions>
            <Border Grid.Column="0" Style="{StaticResource PanelBorder}" Padding="0"><Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                <Grid Margin="12"><TextBlock Text="拍摄清单" Style="{StaticResource SectionTitleText}"/><Button Content="新建" Style="{StaticResource GhostButton}" Command="{Binding NewShotCommand}" HorizontalAlignment="Right" ToolTip="新建拍摄" AutomationProperties.Name="新建拍摄"/></Grid>
                <StackPanel Grid.Row="1" Margin="12,0,12,8"><TextBox Text="{Binding SearchText,UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="搜索拍摄标题和备注"/><WrapPanel Margin="0,7,0,0"><Button Content="全部" Style="{StaticResource GhostButton}" Click="FilterAll_Click" AutomationProperties.Name="显示全部拍摄"/><Button Content="待拍" Style="{StaticResource GhostButton}" Click="FilterPending_Click" AutomationProperties.Name="筛选待拍拍摄"/><Button Content="已拍" Style="{StaticResource GhostButton}" Click="FilterCompleted_Click" AutomationProperties.Name="筛选已拍拍摄"/></WrapPanel></StackPanel>
                <ListBox Grid.Row="2" x:Name="ShotList" ItemsSource="{Binding ShotsView}" SelectedItem="{Binding SelectedShot}" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" AllowDrop="True" PreviewMouseLeftButtonDown="ShotList_PreviewMouseLeftButtonDown" MouseMove="ShotList_MouseMove" DragOver="ShotList_DragOver" Drop="ShotList_Drop" AutomationProperties.Name="项目拍摄顺序列表"><ListBox.ItemTemplate><DataTemplate><Grid Margin="4,3" MinHeight="60"><Grid.ColumnDefinitions><ColumnDefinition Width="38"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="{Binding Order,StringFormat={}{0:00}}" Foreground="{DynamicResource TextTertiaryBrush}" FontSize="16" Margin="5,7,0,0"/><StackPanel Grid.Column="1" Margin="3,5"><TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/><TextBlock Style="{StaticResource CaptionText}" TextTrimming="CharacterEllipsis"><Run Text="{Binding Scene,TargetNullValue=未填写场景}"/><Run Text="  ·  "/><Run Text="{Binding EstimatedMinutes}"/><Run Text=" 分钟"/></TextBlock><TextBlock Loaded="ShotStatus_Loaded" Style="{StaticResource CaptionText}"/></StackPanel></Grid></DataTemplate></ListBox.ItemTemplate><ListBox.ContextMenu><ContextMenu><MenuItem Header="设为当前拍摄" Command="{Binding SetCurrentShotCommand}"/><MenuItem Header="复制拍摄" Command="{Binding DuplicateShotCommand}"/><MenuItem Header="上移" Command="{Binding MoveShotUpCommand}"/><MenuItem Header="下移" Command="{Binding MoveShotDownCommand}"/><Separator/><MenuItem Header="移除当前策划" Command="{Binding ArchiveShotCommand}"/></ContextMenu></ListBox.ContextMenu></ListBox>
                <TextBlock Grid.Row="2" Text="尚未建立拍摄清单" HorizontalAlignment="Center" VerticalAlignment="Center"><TextBlock.Style><Style TargetType="TextBlock" BasedOn="{StaticResource CaptionText}"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding HasShots}" Value="False"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></TextBlock.Style></TextBlock>
                <TextBlock Grid.Row="3" Text="拖动拍摄调整顺序 · 右键查看更多操作" Style="{StaticResource CaptionText}" Margin="12,8" TextWrapping="Wrap"/>
            </Grid></Border>
            <GridSplitter Grid.Column="1" Width="7" HorizontalAlignment="Stretch" Background="Transparent" ResizeDirection="Columns" ResizeBehavior="PreviousAndNext" AutomationProperties.Name="调整拍摄清单宽度"/>
            <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"><StackPanel Margin="14,0">
                <Grid><StackPanel><TextBlock Text="{Binding ShotHeading}" FontSize="20" FontWeight="SemiBold"/><TextBlock Text="图片优先的现场拍摄计划" Style="{StaticResource CaptionText}" Margin="0,4,0,0"/></StackPanel><WrapPanel HorizontalAlignment="Right"><Button Content="{Binding CapturedAssetText}" Style="{StaticResource GhostButton}" Command="{Binding ViewCapturedAssetsCommand}" AutomationProperties.Name="查看当前项目已拍素材"/><Button Content="设为当前拍摄" Style="{StaticResource SecondaryButton}" Command="{Binding SetCurrentShotCommand}" Margin="6,0,0,0" AutomationProperties.Name="设为当前拍摄"/><Button Content="已拍" Style="{StaticResource GhostButton}" Command="{Binding MarkShotCompletedCommand}" Margin="6,0,0,0" AutomationProperties.Name="标记当前拍摄已拍"/><Button Content="跳过" Style="{StaticResource GhostButton}" Command="{Binding MarkShotSkippedCommand}" Margin="6,0,0,0" AutomationProperties.Name="跳过当前拍摄"/></WrapPanel></Grid>
                <Grid Margin="0,12,0,0"><TextBlock Text="当前参考" Style="{StaticResource SectionTitleText}"/><WrapPanel HorizontalAlignment="Right"><Button Content="添加参考" Style="{StaticResource GhostButton}" Command="{Binding AddLocalReferenceCommand}" ToolTip="关联本地参考图，不复制源文件" AutomationProperties.Name="为当前拍摄添加参考"/></WrapPanel></Grid>
                <Border Margin="0,8,0,4" Height="330" Background="{DynamicResource SurfaceSecondaryBrush}" CornerRadius="{StaticResource CardCornerRadius}"><Grid><Image Source="{Binding SelectedReference.ExternalReference}" Stretch="Uniform" Margin="14"/><StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding SelectedReference,Converter={StaticResource NullToVisibilityConverter}}"><TextBlock Text="当前拍摄还没有参考" FontSize="17" FontWeight="SemiBold" HorizontalAlignment="Center"/><TextBlock Text="从下方选择参考，或添加一张本地图片" Style="{StaticResource CaptionText}" Margin="0,6,0,0"/></StackPanel><Border VerticalAlignment="Top" HorizontalAlignment="Left" Margin="12" Padding="6,3" Background="{DynamicResource SurfaceElevatedBrush}" CornerRadius="{StaticResource CompactCornerRadius}"><TextBlock Text="{Binding SelectedReference.Title,TargetNullValue=未选择参考}" Style="{StaticResource CaptionText}"/></Border></Grid></Border>
                <WrapPanel Margin="0,0,0,8"><ToggleButton Content="全部" Style="{StaticResource FilterToggleButton}" Click="ReferenceFilterAll_Click"/><ToggleButton Content="灯位" Style="{StaticResource FilterToggleButton}" Click="ReferenceFilterLighting_Click"/><ToggleButton Content="姿势" Style="{StaticResource FilterToggleButton}" Click="ReferenceFilterPose_Click"/><ToggleButton Content="分镜" Style="{StaticResource FilterToggleButton}" Click="ReferenceFilterStoryboard_Click"/><ToggleButton Content="造型" Style="{StaticResource FilterToggleButton}" Click="ReferenceFilterStyling_Click"/></WrapPanel>
                <ItemsControl ItemsSource="{Binding ReferencesView}" Margin="0,4,0,0"><ItemsControl.ItemsPanel><ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate></ItemsControl.ItemsPanel><ItemsControl.ItemTemplate><DataTemplate><Border Width="150" Height="132" Margin="0,0,8,8" Background="{DynamicResource SurfaceSecondaryBrush}" CornerRadius="{StaticResource CardCornerRadius}" MouseLeftButtonDown="ReferenceCard_MouseLeftButtonDown"><Border.ContextMenu><ContextMenu Style="{StaticResource Av2ContextMenu}"><MenuItem Style="{StaticResource Av2ContextMenuItem}" Header="查看大图" Command="{Binding DataContext.OpenQuickPreviewCommand,RelativeSource={RelativeSource AncestorType=UserControl}}"/><MenuItem Style="{StaticResource Av2ContextMenuItem}" Header="固定参考" Command="{Binding DataContext.TogglePinCommand,RelativeSource={RelativeSource AncestorType=UserControl}}"/><Separator/><MenuItem Style="{StaticResource Av2ContextMenuItem}" Header="从当前拍摄移除" Command="{Binding DataContext.RemoveReferenceCommand,RelativeSource={RelativeSource AncestorType=UserControl}}"/></ContextMenu></Border.ContextMenu><Grid><Image Source="{Binding ExternalReference}" Stretch="Uniform" Margin="5"/><Border VerticalAlignment="Top" HorizontalAlignment="Left" Margin="6" Padding="5,2" Background="{DynamicResource SurfaceElevatedBrush}" CornerRadius="{StaticResource CompactCornerRadius}"><TextBlock Loaded="ReferenceKind_Loaded" Style="{StaticResource CaptionText}"/></Border><Border VerticalAlignment="Bottom" Background="{DynamicResource OverlayBrush}" Padding="7,5"><TextBlock Text="{Binding Title,TargetNullValue=未命名参考}" TextTrimming="CharacterEllipsis"/></Border></Grid></Border></DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
                <Expander Header="策划摘要" Style="{StaticResource CompactExpander}" Margin="0,12,0,0"><StackPanel><TextBlock Text="{Binding ShootGoal,TargetNullValue=尚未填写拍摄目标}" Style="{StaticResource CaptionText}" TextWrapping="Wrap"/><TextBlock Text="{Binding Keywords,TargetNullValue=尚未填写关键词}" Style="{StaticResource CaptionText}" Margin="0,4,0,0"/><TextBlock Text="拍摄目标" Style="{StaticResource CaptionText}" Margin="0,10,0,4"/><TextBox Text="{Binding ShootGoal,UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap" AutomationProperties.Name="拍摄目标"/><TextBlock Text="核心关键词" Style="{StaticResource CaptionText}" Margin="0,8,0,4"/><TextBox Text="{Binding Keywords,UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="核心关键词"/><Grid Margin="0,8,0,0"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="12"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="客户要求" Style="{StaticResource CaptionText}"/><TextBox Text="{Binding ClientRequirements,UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="72" TextWrapping="Wrap" AutomationProperties.Name="客户要求"/></StackPanel><StackPanel Grid.Column="2"><TextBlock Text="必拍内容" Style="{StaticResource CaptionText}"/><TextBox Text="{Binding MustCapture,UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="72" TextWrapping="Wrap" AutomationProperties.Name="必拍内容"/></StackPanel></Grid></StackPanel></Expander>
                <Border Style="{StaticResource PanelBorder}" Margin="0,10,0,16"><StackPanel><TextBlock Text="视觉方向" Style="{StaticResource SectionTitleText}"/><TextBlock Text="目标影调" Style="{StaticResource CaptionText}" Margin="0,10,0,5"/><ItemsControl ItemsSource="{Binding ToneZones}"><ItemsControl.ItemsPanel><ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate></ItemsControl.ItemsPanel><ItemsControl.ItemTemplate><DataTemplate><StackPanel Width="24" VerticalAlignment="Bottom"><Border Height="{Binding BarHeight}" Background="{DynamicResource AccentSoftBrush}" VerticalAlignment="Bottom"/><TextBlock Text="{Binding Zone}" Style="{StaticResource CaptionText}" HorizontalAlignment="Center"/></StackPanel></DataTemplate></ItemsControl.ItemTemplate></ItemsControl><TextBlock Text="项目配色" Style="{StaticResource CaptionText}" Margin="0,10,0,5"/><ItemsControl ItemsSource="{Binding PaletteColors}"><ItemsControl.ItemsPanel><ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate></ItemsControl.ItemsPanel><ItemsControl.ItemTemplate><DataTemplate><Border Width="88" Margin="0,0,6,6" Padding="7" Background="{DynamicResource SurfaceSecondaryBrush}" CornerRadius="{StaticResource CompactCornerRadius}"><StackPanel><Border Height="28" Background="{Binding Hex}" CornerRadius="{StaticResource CompactCornerRadius}"/><TextBlock Text="{Binding Hex}" Style="{StaticResource CaptionText}" Margin="0,4,0,0"/></StackPanel></Border></DataTemplate></ItemsControl.ItemTemplate></ItemsControl><TextBlock Text="项目色彩方案" Style="{StaticResource CaptionText}" Margin="0,10,0,5"/><ItemsControl ItemsSource="{Binding ColorSchemes}"><ItemsControl.ItemTemplate><DataTemplate><TextBlock Text="{Binding Name,StringFormat=• {0}}"/></DataTemplate></ItemsControl.ItemTemplate></ItemsControl><Grid Margin="0,12,0,0"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="12"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="项目灵感板" FontWeight="SemiBold"/><ItemsControl ItemsSource="{Binding ProjectBoards}" Margin="0,5,0,0"><ItemsControl.ItemTemplate><DataTemplate><TextBlock Text="{Binding Name,StringFormat=• {0}}" Style="{StaticResource CaptionText}"/></DataTemplate></ItemsControl.ItemTemplate></ItemsControl></StackPanel><StackPanel Grid.Column="2"><TextBlock Text="项目自由画布" FontWeight="SemiBold"/><ItemsControl ItemsSource="{Binding ProjectCanvases}" Margin="0,5,0,0"><ItemsControl.ItemTemplate><DataTemplate><TextBlock Text="{Binding Name,StringFormat=• {0}}" Style="{StaticResource CaptionText}"/></DataTemplate></ItemsControl.ItemTemplate></ItemsControl></StackPanel></Grid></StackPanel></Border>
            </StackPanel></ScrollViewer>
            <GridSplitter Grid.Column="3" Width="7" HorizontalAlignment="Stretch" Background="Transparent" ResizeDirection="Columns" ResizeBehavior="PreviousAndNext" AutomationProperties.Name="调整策划检查器宽度"/>
            <Border Grid.Column="4" Style="{StaticResource PanelBorder}" Padding="0"><ScrollViewer VerticalScrollBarVisibility="Auto"><StackPanel Margin="14"><Grid><TextBlock Text="{Binding InspectorHeading}" Style="{StaticResource SectionTitleText}"/><Button Grid.Column="1" Content="{Binding InspectorEditText}" Style="{StaticResource GhostButton}" Command="{Binding ToggleInspectorEditCommand}" HorizontalAlignment="Right" ToolTip="{Binding InspectorEditTooltip}" AutomationProperties.Name="编辑或完成拍摄信息"/></Grid><TextBlock Text="当前选择" Style="{StaticResource CaptionText}" Margin="0,4,0,12"/>
                <Expander Header="拍摄信息" IsExpanded="True" AutomationProperties.Name="拍摄信息工具组"><StackPanel Margin="0,8"><StackPanel Visibility="{Binding IsInspectorEditing,Converter={StaticResource BooleanToVisibilityConverter}}"><TextBlock Text="标题" Style="{StaticResource CaptionText}"/><TextBox Text="{Binding ShotTitle,UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="当前拍摄标题"/><TextBlock Text="场景 / 地点" Style="{StaticResource CaptionText}" Margin="0,7,0,0"/><TextBox Text="{Binding ShotScene,UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="当前拍摄场景"/><TextBlock Text="预计分钟" Style="{StaticResource CaptionText}" Margin="0,7,0,0"/><TextBox Text="{Binding ShotEstimatedMinutes,UpdateSourceTrigger=LostFocus}" AutomationProperties.Name="当前拍摄预计分钟"/><TextBlock Text="拍摄备注" Style="{StaticResource CaptionText}" Margin="0,7,0,0"/><TextBox Text="{Binding ShotNotes,UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="86" TextWrapping="Wrap" AutomationProperties.Name="当前拍摄备注"/></StackPanel><StackPanel Visibility="{Binding IsInspectorEditing,Converter={StaticResource InverseBooleanToVisibilityConverter}}"><TextBlock Text="{Binding ShotTitle}" FontSize="17" FontWeight="SemiBold"/><TextBlock Text="{Binding ShotScene,TargetNullValue=未填写场景}" Style="{StaticResource CaptionText}" Margin="0,4,0,0"/><TextBlock Text="{Binding ShotEstimatedMinutes,StringFormat=预计 {0} 分钟}" Style="{StaticResource CaptionText}" Margin="0,4,0,0"/><TextBlock Text="{Binding ShotNotes,TargetNullValue=尚未填写备注}" TextWrapping="Wrap" Style="{StaticResource CaptionText}" Margin="0,8,0,0"/></StackPanel></StackPanel></Expander>
                <Expander Header="参考信息" IsExpanded="True" AutomationProperties.Name="参考信息工具组"><StackPanel Margin="0,8"><TextBlock Text="{Binding SelectedReference.Title,TargetNullValue=选择一张拍摄参考}"/><TextBox Text="{Binding ReferenceNote,UpdateSourceTrigger=LostFocus}" TextWrapping="Wrap" AcceptsReturn="True" Height="64" Margin="0,5,0,0" AutomationProperties.Name="当前参考备注"/><WrapPanel Margin="0,8,0,0"><Button Content="查看大图" Style="{StaticResource GhostButton}" Command="{Binding OpenQuickPreviewCommand}" ToolTip="查看拍摄参考大图" AutomationProperties.Name="查看拍摄参考大图"/><Button Content="固定" Style="{StaticResource GhostButton}" Command="{Binding TogglePinCommand}" Margin="6,0,0,0" ToolTip="固定参考" AutomationProperties.Name="固定当前拍摄参考"/><Button Content="上移" Style="{StaticResource GhostButton}" Command="{Binding MoveReferenceUpCommand}" Margin="6,0,0,0" AutomationProperties.Name="当前参考上移"/><Button Content="下移" Style="{StaticResource GhostButton}" Command="{Binding MoveReferenceDownCommand}" Margin="6,0,0,0" AutomationProperties.Name="当前参考下移"/></WrapPanel></StackPanel></Expander>
                <Button Content="项目视觉资料" Style="{StaticResource SecondaryButton}" Command="{Binding ToggleSourceDrawerCommand}" Margin="0,12,0,0" AutomationProperties.Name="打开项目视觉资料抽屉"/>
            </StackPanel></ScrollViewer></Border>
        </Grid>
        <Border Grid.RowSpan="2" HorizontalAlignment="Right" Width="360" Margin="0,76,12,12" Background="{DynamicResource SurfaceElevatedBrush}" BorderBrush="{DynamicResource DividerBrush}" BorderThickness="1" CornerRadius="{StaticResource RadiusDrawer}" Panel.ZIndex="20"><Border.Style><Style TargetType="Border"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding IsSourceDrawerOpen}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></Border.Style><Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions><Grid Margin="14"><TextBlock Text="项目视觉资料" Style="{StaticResource SectionTitleText}"/><Button Content="关闭" Style="{StaticResource GhostButton}" Command="{Binding ToggleSourceDrawerCommand}" HorizontalAlignment="Right" AutomationProperties.Name="关闭项目视觉资料抽屉"/></Grid><ScrollViewer Grid.Row="1"><StackPanel Margin="14,0,14,14"><TextBlock Text="从素材库、灵感板或自由画布选择参考，然后加入当前拍摄。" TextWrapping="Wrap" Style="{StaticResource CaptionText}"/><TextBlock Text="素材库" FontWeight="SemiBold" Margin="0,16,0,4"/><TextBlock Text="打开素材库并按当前项目筛选" Style="{StaticResource CaptionText}"/><TextBlock Text="灵感板" FontWeight="SemiBold" Margin="0,16,0,4"/><ItemsControl ItemsSource="{Binding ProjectBoards}"><ItemsControl.ItemTemplate><DataTemplate><TextBlock Text="{Binding Name,StringFormat=• {0}}"/></DataTemplate></ItemsControl.ItemTemplate></ItemsControl><TextBlock Text="自由画布" FontWeight="SemiBold" Margin="0,16,0,4"/><ItemsControl ItemsSource="{Binding ProjectCanvases}"><ItemsControl.ItemTemplate><DataTemplate><TextBlock Text="{Binding Name,StringFormat=• {0}}"/></DataTemplate></ItemsControl.ItemTemplate></ItemsControl></StackPanel></ScrollViewer></Grid></Border>
        <Border Grid.RowSpan="2" Width="760" Height="560" MaxWidth="{Binding ActualWidth,RelativeSource={RelativeSource AncestorType=UserControl}}" MaxHeight="{Binding ActualHeight,RelativeSource={RelativeSource AncestorType=UserControl}}" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="12" Background="{DynamicResource SurfaceElevatedBrush}" BorderBrush="{DynamicResource DividerBrush}" BorderThickness="1" CornerRadius="{StaticResource DialogCornerRadius}" Panel.ZIndex="30"><Border.Style><Style TargetType="Border"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding IsQuickPreviewOpen}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></Border.Style><Grid><Image Source="{Binding SelectedReference.ExternalReference}" Stretch="Uniform"/><Button Content="关闭" Style="{StaticResource GhostButton}" Command="{Binding CloseQuickPreviewCommand}" HorizontalAlignment="Right" VerticalAlignment="Top" AutomationProperties.Name="关闭拍摄参考大图"/><Button Content="上一张" Style="{StaticResource GhostButton}" HorizontalAlignment="Left" VerticalAlignment="Center" Click="PreviousReference_Click" AutomationProperties.Name="查看上一张拍摄参考"/><Button Content="下一张" Style="{StaticResource GhostButton}" HorizontalAlignment="Right" VerticalAlignment="Center" Click="NextReference_Click" AutomationProperties.Name="查看下一张拍摄参考"/></Grid></Border>
    </Grid>
    <Grid Margin="24" Visibility="{Binding IsOverview,Converter={StaticResource BooleanToVisibilityConverter}}" AutomationProperties.Name="策划项目总览">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <StackPanel><TextBlock Text="策划中心" Style="{StaticResource PageTitleText}"/><TextBlock Text="{Binding OverviewStatus}" Style="{StaticResource CaptionText}" Margin="0,6,0,16"/></StackPanel>
        <WrapPanel Grid.Row="1" Margin="0,0,0,18">
            <TextBox Width="240" Text="{Binding ProjectSearch,UpdateSourceTrigger=PropertyChanged}" ToolTip="搜索项目名称" AutomationProperties.Name="搜索策划项目"/>
            <ComboBox Width="150" ItemsSource="{Binding OverviewFilters}" SelectedItem="{Binding OverviewFilter}" Margin="12,0" AutomationProperties.Name="策划项目筛选"/>
            <TextBox Width="220" Text="{Binding NewPlanningName,UpdateSourceTrigger=PropertyChanged}" ToolTip="新策划项目名称" AutomationProperties.Name="新策划项目名称"/>
            <Button Content="新建策划" AutomationProperties.Name="新建策划" Command="{Binding CreatePlanningCommand}" Style="{StaticResource PrimaryButton}" Margin="10,0,0,0"/>
        </WrapPanel>
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding PlanningProjectsView}">
                <ItemsControl.ItemsPanel><ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate><DataTemplate>
                    <Button Width="280" Margin="0,0,16,16" Padding="0" Style="{StaticResource GhostButton}"
                            Command="{Binding DataContext.OpenPlanningProjectCommand,RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" AutomationProperties.Name="{Binding Name}">
                        <StackPanel Width="278">
                            <Grid Height="160" Background="{DynamicResource SurfaceSecondaryBrush}">
                                <TextBlock Text="拍摄策划" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource CaptionText}"/>
                                <Image Source="{Binding Cover}" Stretch="UniformToFill" ClipToBounds="True"/>
                            </Grid>
                            <StackPanel Margin="12"><TextBlock Text="{Binding Name}" FontSize="16" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/><TextBlock Text="{Binding DateText}" Style="{StaticResource CaptionText}" Margin="0,6,0,0"/><TextBlock Text="{Binding Progress}" Style="{StaticResource CaptionText}" Margin="0,4,0,0"/></StackPanel>
                        </StackPanel>
                    </Button>
                </DataTemplate></ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
    </Grid>
</UserControl>

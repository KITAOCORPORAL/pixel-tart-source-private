<Window x:Class="PixelTart.AssetLibrary.Preview.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:PixelTart.AssetLibrary.Preview"
        Title="像素蛋挞 · Asset Library V1.5 Preview" Width="1560" Height="940" MinWidth="1180" MinHeight="720" Loaded="OnLoaded" Closed="OnClosed">
    <Grid Margin="14">
        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="10" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="330" /><ColumnDefinition Width="Auto" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
            <StackPanel><TextBlock Text="Pixel Tart Asset Library" FontSize="25" FontWeight="SemiBold" /><TextBlock Text="素材库 V1.5 · Reference Import 默认不移动、不改名、不删除源文件" Style="{StaticResource Caption}" Margin="0,3,0,0" /></StackPanel>
            <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" ToolTip="搜索名称、标签、文件夹与备注（280ms debounce）" AutomationProperties.AutomationId="AssetLibrarySearch" VerticalAlignment="Center" />
            <Button Grid.Column="2" Content="+ 筛选" Margin="8,0,4,0" Command="{Binding RefreshCommand}" />
            <Button Grid.Column="3" Content="导入引用" Style="{StaticResource PrimaryButton}" Command="{Binding ImportCommand}" AutomationProperties.AutomationId="AssetLibraryImport" />
        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions><ColumnDefinition Width="262" /><ColumnDefinition Width="10" /><ColumnDefinition Width="*" /><ColumnDefinition Width="10" /><ColumnDefinition Width="350" /></Grid.ColumnDefinitions>
            <Border Style="{StaticResource Panel}">
                <DockPanel><StackPanel DockPanel.Dock="Top">
                    <TextBlock Text="全部素材" FontWeight="SemiBold" /><UniformGrid Columns="2" Margin="0,7,0,10"><Button Content="最近添加" Command="{Binding RefreshCommand}" /><Button Content="高评分" Command="{Binding RefreshCommand}" /></UniformGrid>
                    <Separator /><Grid Margin="0,7,0,0"><TextBlock Text="文件夹" Style="{StaticResource Section}" /><StackPanel Orientation="Horizontal" HorizontalAlignment="Right"><Button Content="+" Command="{Binding NewFolderCommand}" /><Button Content="子" Command="{Binding NewSubfolderCommand}" /><Button Content="批量" Command="{Binding BatchFolderCommand}" /></StackPanel></Grid>
                    <TextBox Text="{Binding NewFolderName, UpdateSourceTrigger=PropertyChanged}" ToolTip="新文件夹名称" Margin="0,6,0,4" />
                    <TextBlock Text="拖放合同：目标高亮 / 非法循环阻止 / 仅 membership" Style="{StaticResource Caption}" TextWrapping="Wrap" />
                </StackPanel>
                <ScrollViewer VerticalScrollBarVisibility="Auto"><StackPanel>
                    <ItemsControl ItemsSource="{Binding FolderTree}" AutomationProperties.AutomationId="AssetFolderTree"><ItemsControl.ItemTemplate><DataTemplate><Border Padding="5,4" Margin="0,1" Background="#191B1C"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="{Binding Path}" TextTrimming="CharacterEllipsis" /><TextBlock Grid.Column="1" Text="{Binding DescendantAssetCount}" Style="{StaticResource Caption}" /></Grid></Border></DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
                    <Separator Margin="0,10" /><TextBlock Text="智能文件夹" Style="{StaticResource Section}" /><ListBox ItemsSource="{Binding SmartFolders}" SelectedItem="{Binding SelectedSmartFolder}" DisplayMemberPath="Name" AutomationProperties.AutomationId="SmartFolders" />
                    <TextBlock Text="标签组 / 常用标签" Style="{StaticResource Section}" Margin="0,12,0,4" /><ItemsControl ItemsSource="{Binding TagGroups}"><ItemsControl.ItemTemplate><DataTemplate><TextBlock Text="{Binding Name}" Padding="5,3" /></DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
                    <ListBox ItemsSource="{Binding Tags}" SelectedItem="{Binding SelectedTag}" DisplayMemberPath="Name" AutomationProperties.AutomationId="AssetTags" />
                </StackPanel></ScrollViewer></DockPanel>
            </Border>
            <Border Grid.Column="2" Style="{StaticResource Panel}">
                <Grid><Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                    <Grid Margin="0,0,0,8"><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /><ColumnDefinition Width="150" /></Grid.ColumnDefinitions><StackPanel Orientation="Horizontal"><TextBlock Text="素材网格" Style="{StaticResource Section}" /><TextBlock Text="{Binding SelectionCount, StringFormat= · 已选 {0}}" Style="{StaticResource Caption}" Margin="8,2,0,0" /></StackPanel><TextBlock Grid.Column="1" Text="Small  ·  Medium  ·  Large" Style="{StaticResource Caption}" VerticalAlignment="Center" /><Slider Grid.Column="2" Minimum="120" Maximum="280" Value="{Binding ThumbnailWidth}" Margin="8,0,0,0" /></Grid>
                    <ListBox x:Name="AssetGrid" Grid.Row="1" ItemsSource="{Binding Assets}" SelectionMode="Extended" SelectionChanged="AssetGrid_SelectionChanged" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto" AutomationProperties.AutomationId="AssetGrid" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling">
                        <ListBox.ItemsPanel><ItemsPanelTemplate><local:VirtualizingWrapPanel ItemWidth="{Binding DataContext.ThumbnailWidth, RelativeSource={RelativeSource AncestorType=Window}}" ItemHeight="{Binding DataContext.ThumbnailWidth, RelativeSource={RelativeSource AncestorType=Window}}" /></ItemsPanelTemplate></ListBox.ItemsPanel>
                        <ListBox.ItemTemplate><DataTemplate><Border Width="{Binding DataContext.ThumbnailWidth, RelativeSource={RelativeSource AncestorType=Window}}" Height="{Binding DataContext.ThumbnailWidth, RelativeSource={RelativeSource AncestorType=Window}}" Background="{StaticResource PanelAltBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="6" Margin="3"><Grid><Grid.RowDefinitions><RowDefinition Height="*" /><RowDefinition Height="36" /></Grid.RowDefinitions><Border Background="#0D0F10"><Image local:AsyncThumbnail.SourcePath="{Binding SourcePath}" local:AsyncThumbnail.DecodeWidth="{Binding DataContext.ThumbnailWidth, RelativeSource={RelativeSource AncestorType=Window}}" Stretch="UniformToFill" /></Border><Grid Grid.Row="1" Margin="2,5,2,0"><Grid.RowDefinitions><RowDefinition /><RowDefinition /></Grid.RowDefinitions><TextBlock Text="{Binding DisplayName}" TextTrimming="CharacterEllipsis" /><TextBlock Grid.Row="1" Text="{Binding Rating, StringFormat=★ {0}}" Style="{StaticResource Caption}" /></Grid></Grid></Border></DataTemplate></ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
            <Border Grid.Column="4" Style="{StaticResource Panel}">
                <ScrollViewer VerticalScrollBarVisibility="Auto"><StackPanel>
                    <DockPanel><TextBlock Text="检查器" Style="{StaticResource Section}" /><TextBlock Text="{Binding SelectionCount, StringFormat={}{0} 项}" Style="{StaticResource Caption}" HorizontalAlignment="Right" /></DockPanel>
                    <Border Height="175" Background="#0D0F10" Margin="0,8,0,8"><Image local:AsyncThumbnail.SourcePath="{Binding SelectedAsset.SourcePath}" local:AsyncThumbnail.DecodeWidth="420" Stretch="Uniform" /></Border>
                    <TextBlock Text="{Binding SelectedAsset.DisplayName, TargetNullValue=请选择素材}" FontSize="16" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                    <TextBlock Text="{Binding SelectedAsset.MediaType, StringFormat=格式：{0}}" Style="{StaticResource Caption}" /><TextBlock Text="{Binding SelectedAsset.FileSize, StringFormat=大小：{0:N0} bytes}" Style="{StaticResource Caption}" /><TextBlock Text="{Binding SelectedAsset.IsMissing, StringFormat=文件已移动：{0}}" Style="{StaticResource Caption}" />
                    <TextBlock Text="所在文件夹" Style="{StaticResource Section}" Margin="0,12,0,4" /><WrapPanel><Button Content="+ 添加文件夹" Command="{Binding AddFolderCommand}" /><Button Content="F 快速分类" Click="OpenClassifier_Click" /></WrapPanel>
                    <TextBlock Text="标签" Style="{StaticResource Section}" Margin="0,12,0,4" /><TextBox x:Name="TagInputBox" Text="{Binding TagInput, UpdateSourceTrigger=PropertyChanged}" ToolTip="逗号、中文逗号或换行批量添加" /><WrapPanel Margin="0,5,0,0"><Button Content="应用标签" Command="{Binding ApplyTagsCommand}" /><Button Content="标签管理" Click="OpenTagManager_Click" /></WrapPanel>
                    <ItemsControl ItemsSource="{Binding SelectedTagSummary}"><ItemsControl.ItemTemplate><DataTemplate><TextBlock><Run Text="{Binding Tag.Name}" /><Run Text="  " /><Run Text="{Binding MembershipCount}" /><Run Text="/" /><Run Text="{Binding SelectedCount}" /></TextBlock></DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
                    <TextBlock Text="评分 / 备注" Style="{StaticResource Section}" Margin="0,12,0,4" /><UniformGrid Columns="6"><Button Content="0" Command="{Binding RateCommand}" CommandParameter="0"/><Button Content="1" Command="{Binding RateCommand}" CommandParameter="1"/><Button Content="2" Command="{Binding RateCommand}" CommandParameter="2"/><Button Content="3" Command="{Binding RateCommand}" CommandParameter="3"/><Button Content="4" Command="{Binding RateCommand}" CommandParameter="4"/><Button Content="5" Command="{Binding RateCommand}" CommandParameter="5"/></UniformGrid>
                    <Separator Margin="0,12" /><Expander Header="视觉分析" IsExpanded="True"><StackPanel><TextBlock Text="{Binding AnalysisStatus}" Style="{StaticResource Caption}" TextWrapping="Wrap" /><TabControl Margin="0,6,0,0" AutomationProperties.AutomationId="VisualAnalysisTabs">
                        <TabItem Header="配色"><StackPanel Margin="7"><DockPanel><TextBlock Text="主色主题" FontWeight="SemiBold" /><ComboBox SelectedValue="{Binding PaletteSize}" SelectedValuePath="Content" HorizontalAlignment="Right"><ComboBoxItem Content="3"/><ComboBoxItem Content="5"/><ComboBoxItem Content="7"/></ComboBox></DockPanel><ItemsControl ItemsSource="{Binding Analysis.Palette}"><ItemsControl.ItemsPanel><ItemsPanelTemplate><StackPanel Orientation="Horizontal" /></ItemsPanelTemplate></ItemsControl.ItemsPanel><ItemsControl.ItemTemplate><DataTemplate><StackPanel Width="50" Margin="2"><Border Height="38" Background="{Binding Hex}" CornerRadius="3"/><TextBlock Text="{Binding Hex}" FontSize="9"/><TextBlock Text="{Binding Weight, StringFormat={}{0:P0}}" Style="{StaticResource Caption}" /></StackPanel></DataTemplate></ItemsControl.ItemTemplate></ItemsControl><TextBlock Text="{Binding Analysis.Harmony, StringFormat=主色结构倾向：{0}}" Margin="0,6,0,0" /><TextBlock Text="{Binding Analysis.AverageSaturation, StringFormat=平均饱和度：{0:P0}}" Style="{StaticResource Caption}"/><TextBlock Text="{Binding Analysis.WarmCool, StringFormat=冷暖倾向：{0}}" Style="{StaticResource Caption}"/></StackPanel></TabItem>
                        <TabItem Header="直方图"><StackPanel Margin="7"><local:HistogramDrawing Analysis="{Binding Analysis}" Height="110" /><TextBlock Text="{Binding Analysis.BlackClipRatio, StringFormat=暗部剪切：{0:P2}}" /><TextBlock Text="{Binding Analysis.WhiteClipRatio, StringFormat=高光剪切：{0:P2}}" /></StackPanel></TabItem>
                        <TabItem Header="影调"><StackPanel Margin="7"><TextBlock Text="{Binding Analysis.ToneKey, StringFormat=影调倾向：{0}}" FontWeight="SemiBold"/><TextBlock Text="{Binding Analysis.Contrast, StringFormat=对比度倾向：{0}}"/><TextBlock Text="{Binding Analysis.LuminanceSpan, StringFormat=画面亮度跨度：{0}}"/><TextBlock Text="{Binding Analysis.ToneZones.DeepShadow, StringFormat=深暗部 {0:P0}}"/><TextBlock Text="{Binding Analysis.ToneZones.Shadow, StringFormat=暗部 {0:P0}}"/><TextBlock Text="{Binding Analysis.ToneZones.Midtone, StringFormat=中间调 {0:P0}}"/><TextBlock Text="{Binding Analysis.ToneZones.Highlight, StringFormat=亮部 {0:P0}}"/><TextBlock Text="{Binding Analysis.ToneZones.Specular, StringFormat=极亮部 {0:P0}}"/><TextBlock Text="基于最终像素的视觉统计；不是准确曝光、真实色温或相机动态范围。" Style="{StaticResource Caption}" TextWrapping="Wrap" Margin="0,7,0,0"/></StackPanel></TabItem>
                    </TabControl></StackPanel></Expander>
                    <Separator Margin="0,12" /><TextBlock Text="Smart Folder Builder" Style="{StaticResource Section}"/><TextBox Text="{Binding SmartFolderName}" Margin="0,5,0,3"/><DockPanel><TextBlock Text="Rating ≥" VerticalAlignment="Center"/><TextBox Text="{Binding SmartRuleValue}" Width="45"/><Button Content="保存为智能文件夹" Command="{Binding SaveSmartFolderCommand}" HorizontalAlignment="Right"/></DockPanel>
                    <WrapPanel Margin="0,10,0,0"><Button Content="重新定位" Command="{Binding RelinkCommand}"/><Button Content="撤销上一项" Command="{Binding UndoCommand}"/></WrapPanel>
                </StackPanel></ScrollViewer>
            </Border>
        </Grid>
        <DockPanel Grid.Row="3" Margin="0,8,0,0"><TextBlock Text="F 分类 · Shift+D 重复分类 · T 标签 · Space 预览 · 0–5 评分 · Ctrl+A 多选 · Esc 清除" Style="{StaticResource Caption}" /><TextBlock Text="{Binding Status}" Style="{StaticResource Caption}" HorizontalAlignment="Right" /></DockPanel>
    </Grid>
</Window>

# 像素蛋挞 2.2.0 阶段 D：提醒与工作台日程报告

## 1. 执行结论

- 阶段 D 已完成：是。
- 功能提交：`3719657ae6fd72b9d1f063d7820e6b27c732710d`（`feat(calendar): add booking reminders and workbench schedule`）。
- 当前开发分支：`release/2.2.0`。
- ProductVersion：`2.2.0`。
- FileVersion：`2.2.0.0`。
- SchemaVersion：`2`。
- 未新增数据库迁移、数据库表或 `ProjectRelationships`。
- 未 Publish、未生成安装包、未合并 main、未创建 Tag、未进入阶段 E。

## 2. 阶段 D 正式基线

- 阶段 D 开始 HEAD：`c9af9ebad8e36fc99d753d330f59a4a3c6b70146`。
- 阶段 D 功能提交的直接父提交：`9f4c1311c913f5b8090799043ff505abcabbc41f`（仅隔离日历数据库测试连接，不改写阶段 C 历史）。
- 阶段 C 功能提交：`db14bf36e58c545e262966f60d23b81eedaed37a`。
- 阶段 C 安全加固提交：`6aa1cd6d5f5b5b117287ad28201a2b5db38111a3`。
- 阶段 C 最终审计补齐提交：`5973e3f1e8b1f9075bb1755a02a7587244cf8f47`。
- 上述三个阶段 C 提交均为阶段 D 功能提交的祖先。
- 阶段 D 开始测试基线：759/759，其中核心 693/693、WPF 39/39、DPI 27/27。

## 3. 提醒调度

- 完成应用内提醒调度器；应用启动后启动，退出时停止并释放，不注册系统服务，软件退出后不常驻。
- 提醒默认关闭；只有用户明确启用的提醒才参与调度。
- 支持相对拍摄开始时间的预设提醒和自定义绝对时间提醒。
- 排期时间变更后，在现有排期保存事务内重新计算相对提醒触发时间。
- 排期归档或取消后关闭对应提醒；恢复归档不会擅自重新启用提醒。
- 提醒状态与通知记录使用同一 SQLite 事务持久化；界面订阅者异常不会造成重复触发。
- 同一提醒通过原子状态领取最多触发一次，已触发、已关闭、已取消和已关闭通知均不会再次补报。

## 4. 错过提醒补报规则

- 只补报当前时刻之前最近 24 小时内错过的提醒。
- 对应拍摄已经结束时不补报。
- 已触发、已关闭、已取消提醒不补报。
- 同一提醒最多触发一次。
- 补报时立即把 `Triggered` 状态和通知记录持久化。
- 系统时间前拨、后拨和调度器重复扫描均由持久状态和原子领取保护。

## 5. 工作台日程

- 完成“今日拍摄”和“未来 7 天”工作台摘要。
- 今日列表覆盖全天、跨天和正在进行中的排期。
- 未来 7 天按本地日期分组，不一次读取全部历史排期。
- 工作台显示权威项目名称、时间、状态、地点摘要、准备清单进度、文档数量和提醒启用状态。
- 工作台支持刷新、今日/未来切换、打开工作日历及从日程项直接打开排期详情。
- 排期保存、归档和恢复后通过既有服务事件刷新工作台，不让页面或 ViewModel 直接访问 SQLite。

## 6. 隐私、安全与阶段 C 保留情况

- 提醒通知内容使用脱敏摘要，不写入客户完整资料、详细地址、文档路径或哈希。
- 审计日志不记录完整文件路径、客户敏感资料或文档哈希。
- 保留全部阶段 C 安全加固：混合批次部分完成、规范化重复路径保护、文件任务终态、重新定位单条 SQL 原子更新、重复目标保护、哈希不一致确认、归档重试保护、跨排期待处理动作隔离、权威 ProjectId、参数化 SQLite、UndoJournal 安全撤销、默认引用原位置、安全复制不覆盖源文件、移除关联不删除电脑文件。
- 阶段 D 功能提交未修改阶段 C 文档工作流、文件操作契约、文件操作执行器、文档 ViewModel 或数据库迁移文件。
- 当前共享工作树中另有来源待确认的阶段 C 恢复候选和未授权天气候选修改；它们全部保留在工作树，未进入阶段 D 功能提交。

## 7. UI、主题、DPI 与无障碍

- 新增排期详情提醒面板、提醒通知宿主、工作台日程视图和工作台摘要控件。
- 覆盖浅色、深色和高对比资源引用检查。
- 覆盖键盘焦点、AutomationProperties、可访问名称和通知操作入口。
- 125%、150%、200% 逻辑 DPI 自动化门禁均通过，无阻断级裁切、重叠或越界。
- `physicalDpiManuallyTested=false` 继续作为允许的已知限制，未把真实物理 DPI 人工测试设为阶段 D 硬门禁。

## 8. 测试结果

- 阶段 D 新增测试数量：50 项。
- 核心测试：727/727（较基线新增 34）。
- WPF 测试：48/48（较基线新增 9）。
- DPI 测试：34/34（较基线新增 7）。
- Debug：809/809，0 跳过，构建 0 警告、0 错误。
- Release：809/809，0 跳过，构建 0 警告、0 错误。
- 最终测试总数：809/809；原 759 项全部保留，未删除、跳过或禁用阶段 C 测试。
- 覆盖提醒默认关闭、CRUD、排期时间重算、归档/取消、24 小时补报边界、拍摄结束排除、终态排除、原子去重、通知持久化、隐私脱敏、今日/未来 7 天、跨日/月/年、时区/DST、文档与提醒摘要、主题、DPI 和无障碍。

## 9. 数据库与发布边界

- 数据库变化：无迁移、无新表、无 SchemaVersion 变化；复用 Schema 2 的 `BookingReminders`、`ShootBookings`、`BookingDocuments` 和既有 `Notifications`。
- 未创建或实现 `ProjectRelationships`。
- 未操作用户正式 LocalAppData 数据库。
- 未生成安装包，未合并 main，未创建 `v2.2.0` Tag。
- 阶段 D 完成后已停止，不自动进入阶段 E。

## 10. 下一阶段建议

- 不建议当前自动进入阶段 E。
- 应先由用户确认并处理共享工作树中未提交的阶段 C 恢复候选和天气候选修改，确保阶段 D 报告提交后的工作树边界清晰，再单独下达阶段 E 指令。
